module.exports = [
"[externals]/next/dist/compiled/next-server/app-route-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-route-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/@opentelemetry/api [external] (next/dist/compiled/@opentelemetry/api, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/@opentelemetry/api", () => require("next/dist/compiled/@opentelemetry/api"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-page-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-page-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-unit-async-storage.external.js [external] (next/dist/server/app-render/work-unit-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-unit-async-storage.external.js", () => require("next/dist/server/app-render/work-unit-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-async-storage.external.js [external] (next/dist/server/app-render/work-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-async-storage.external.js", () => require("next/dist/server/app-render/work-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/shared/lib/no-fallback-error.external.js [external] (next/dist/shared/lib/no-fallback-error.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/shared/lib/no-fallback-error.external.js", () => require("next/dist/shared/lib/no-fallback-error.external.js"));

module.exports = mod;
}),
"[project]/src/lib/prisma.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "prisma",
    ()=>prisma
]);
var __TURBOPACK__imported__module__$5b$externals$5d2f40$prisma$2f$client__$5b$external$5d$__$2840$prisma$2f$client$2c$__cjs$2c$__$5b$project$5d2f$node_modules$2f40$prisma$2f$client$29$__ = __turbopack_context__.i("[externals]/@prisma/client [external] (@prisma/client, cjs, [project]/node_modules/@prisma/client)");
;
const globalForPrisma = /*TURBOPACK member replacement*/ __turbopack_context__.g;
const prisma = globalForPrisma.prisma || new __TURBOPACK__imported__module__$5b$externals$5d2f40$prisma$2f$client__$5b$external$5d$__$2840$prisma$2f$client$2c$__cjs$2c$__$5b$project$5d2f$node_modules$2f40$prisma$2f$client$29$__["PrismaClient"]({
    log: [
        "warn",
        "error"
    ]
});
if ("TURBOPACK compile-time truthy", 1) globalForPrisma.prisma = prisma;
}),
"[externals]/next/dist/server/app-render/after-task-async-storage.external.js [external] (next/dist/server/app-render/after-task-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/after-task-async-storage.external.js", () => require("next/dist/server/app-render/after-task-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/crypto [external] (crypto, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("crypto", () => require("crypto"));

module.exports = mod;
}),
"[externals]/assert [external] (assert, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("assert", () => require("assert"));

module.exports = mod;
}),
"[externals]/util [external] (util, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("util", () => require("util"));

module.exports = mod;
}),
"[externals]/zlib [external] (zlib, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("zlib", () => require("zlib"));

module.exports = mod;
}),
"[externals]/fs [external] (fs, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("fs", () => require("fs"));

module.exports = mod;
}),
"[externals]/stream [external] (stream, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("stream", () => require("stream"));

module.exports = mod;
}),
"[externals]/child_process [external] (child_process, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("child_process", () => require("child_process"));

module.exports = mod;
}),
"[externals]/events [external] (events, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("events", () => require("events"));

module.exports = mod;
}),
"[externals]/os [external] (os, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("os", () => require("os"));

module.exports = mod;
}),
"[externals]/path [external] (path, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("path", () => require("path"));

module.exports = mod;
}),
"[externals]/url [external] (url, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("url", () => require("url"));

module.exports = mod;
}),
"[externals]/async_hooks [external] (async_hooks, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("async_hooks", () => require("async_hooks"));

module.exports = mod;
}),
"[externals]/fs/promises [external] (fs/promises, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("fs/promises", () => require("fs/promises"));

module.exports = mod;
}),
"[externals]/https [external] (https, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("https", () => require("https"));

module.exports = mod;
}),
"[externals]/http [external] (http, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("http", () => require("http"));

module.exports = mod;
}),
"[externals]/net [external] (net, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("net", () => require("net"));

module.exports = mod;
}),
"[externals]/tls [external] (tls, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("tls", () => require("tls"));

module.exports = mod;
}),
"[externals]/buffer [external] (buffer, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("buffer", () => require("buffer"));

module.exports = mod;
}),
"[project]/src/modules/whatsapp/auth/usePrismaAuthState.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

return __turbopack_context__.a(async (__turbopack_handle_async_dependencies__, __turbopack_async_result__) => { try {

__turbopack_context__.s([
    "usePrismaAuthState",
    ()=>usePrismaAuthState
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/prisma.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/@whiskeysockets/baileys/lib/index.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$generics$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@whiskeysockets/baileys/lib/Utils/generics.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$auth$2d$utils$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@whiskeysockets/baileys/lib/Utils/auth-utils.js [app-route] (ecmascript)");
var __turbopack_async_dependencies__ = __turbopack_handle_async_dependencies__([
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__,
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$generics$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__,
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$auth$2d$utils$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__
]);
[__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$generics$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$auth$2d$utils$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__] = __turbopack_async_dependencies__.then ? (await __turbopack_async_dependencies__)() : __turbopack_async_dependencies__;
;
;
const usePrismaAuthState = async (sessionId)=>{
    // Helper to read JSON with Buffer handling
    const readData = async (type, id)=>{
        try {
            const key = `${type}-${id}`;
            const data = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].authState.findUnique({
                where: {
                    sessionId_key: {
                        sessionId,
                        key
                    }
                }
            });
            if (data && data.value) {
                return JSON.parse(JSON.stringify(data.value), __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$generics$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["BufferJSON"].reviver);
            }
            return null;
        } catch (error) {
            console.error('Error reading auth state:', error);
            return null;
        }
    };
    // Helper to write data
    const writeData = async (type, id, data)=>{
        try {
            const key = `${type}-${id}`;
            const value = JSON.parse(JSON.stringify(data, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$generics$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["BufferJSON"].replacer));
            await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].authState.upsert({
                where: {
                    sessionId_key: {
                        sessionId,
                        key
                    }
                },
                create: {
                    sessionId,
                    key,
                    value
                },
                update: {
                    value
                }
            });
        } catch (error) {
            console.error('Error writing auth state:', error);
        }
    };
    const removeData = async (type, id)=>{
        try {
            const key = `${type}-${id}`;
            await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].authState.deleteMany({
                where: {
                    sessionId,
                    key
                }
            });
        } catch (error) {
        // ignore
        }
    };
    const creds = await readData('creds', 'me') || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$auth$2d$utils$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["initAuthCreds"])();
    return {
        state: {
            creds,
            keys: {
                get: async (type, ids)=>{
                    const data = {};
                    await Promise.all(ids.map(async (id)=>{
                        let value = await readData(type, id);
                        if (type === 'app-state-sync-key' && value) {
                            value = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$generics$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["BufferJSON"].reviver(null, value);
                        }
                        if (value) {
                            data[id] = value;
                        }
                    }));
                    return data;
                },
                set: async (data)=>{
                    const tasks = [];
                    for(const category in data){
                        const categoryData = data[category];
                        if (!categoryData) continue;
                        for(const id in categoryData){
                            const value = categoryData[id];
                            if (value) {
                                tasks.push(writeData(category, id, value));
                            } else {
                                tasks.push(removeData(category, id));
                            }
                        }
                    }
                    await Promise.all(tasks);
                }
            }
        },
        saveCreds: async ()=>{
            await writeData('creds', 'me', creds);
        }
    };
};
__turbopack_async_result__();
} catch(e) { __turbopack_async_result__(e); } }, false);}),
"[project]/src/lib/jid-utils.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "batchResolveToPhoneJid",
    ()=>batchResolveToPhoneJid,
    "isLidJid",
    ()=>isLidJid,
    "normalizeJid",
    ()=>normalizeJid,
    "resolveToPhoneJid",
    ()=>resolveToPhoneJid,
    "resolveToPhoneJidBySessionId",
    ()=>resolveToPhoneJidBySessionId
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/prisma.ts [app-route] (ecmascript)");
;
function isLidJid(jid) {
    if (!jid) return false;
    return jid.endsWith("@lid");
}
function normalizeJid(jid) {
    if (!jid) return "";
    if (jid.endsWith("@c.us")) {
        return jid.replace("@c.us", "@s.whatsapp.net");
    }
    return jid;
}
async function resolveToPhoneJid(jid, dbSessionId, inlineAlt) {
    if (!jid) return jid;
    // Only resolve if it's a @lid JID
    if (!isLidJid(jid)) return jid;
    // 1. Use inline remoteJidAlt if provided
    if (inlineAlt && !isLidJid(inlineAlt)) {
        return inlineAlt;
    }
    // 2. Try DB lookup
    try {
        const contact = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].contact.findFirst({
            where: {
                sessionId: dbSessionId,
                OR: [
                    {
                        jid: jid
                    },
                    {
                        lid: jid
                    }
                ]
            },
            select: {
                jid: true,
                remoteJidAlt: true,
                lid: true
            }
        });
        if (contact) {
            // If remoteJidAlt is a phone JID, use it
            if (contact.remoteJidAlt && !isLidJid(contact.remoteJidAlt)) {
                return contact.remoteJidAlt;
            }
            // If the primary jid is a phone JID, use it
            if (contact.jid && !isLidJid(contact.jid)) {
                return contact.jid;
            }
        }
    } catch (e) {
        console.error(`resolveToPhoneJid: DB lookup failed for ${jid}`, e);
    }
    // 3. Fallback: return original JID
    return jid;
}
async function resolveToPhoneJidBySessionId(jid, sessionId, inlineAlt) {
    if (!jid || !isLidJid(jid)) return jid;
    // 1. Use inline remoteJidAlt if provided
    if (inlineAlt && !isLidJid(inlineAlt)) {
        return inlineAlt;
    }
    try {
        const session = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findUnique({
            where: {
                sessionId
            },
            select: {
                id: true
            }
        });
        if (session) {
            return resolveToPhoneJid(jid, session.id, inlineAlt);
        }
    } catch (e) {
        console.error(`resolveToPhoneJidBySessionId: Session lookup failed for ${sessionId}`, e);
    }
    return jid;
}
async function batchResolveToPhoneJid(jids, dbSessionId) {
    const result = new Map();
    const lids = jids.filter((j)=>isLidJid(j));
    // No LIDs to resolve, return identity map
    if (lids.length === 0) {
        jids.forEach((j)=>result.set(j, j));
        return result;
    }
    // Batch query: find contacts that match any of the LID JIDs
    try {
        const contacts = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].contact.findMany({
            where: {
                sessionId: dbSessionId,
                OR: [
                    {
                        jid: {
                            in: lids
                        }
                    },
                    {
                        lid: {
                            in: lids
                        }
                    }
                ]
            },
            select: {
                jid: true,
                lid: true,
                remoteJidAlt: true
            }
        });
        // Build lookup map
        for (const c of contacts){
            const phoneJid = c.remoteJidAlt && !isLidJid(c.remoteJidAlt) ? c.remoteJidAlt : !isLidJid(c.jid) ? c.jid : null;
            if (phoneJid) {
                // Map by jid (if it's a LID)
                if (isLidJid(c.jid)) {
                    result.set(c.jid, phoneJid);
                }
                // Map by lid field
                if (c.lid && isLidJid(c.lid)) {
                    result.set(c.lid, phoneJid);
                }
            }
        }
    } catch (e) {
        console.error(`batchResolveToPhoneJid: DB query failed`, e);
    }
    // Fill in any un-resolved JIDs with themselves
    for (const jid of jids){
        if (!result.has(jid)) {
            result.set(jid, jid);
        }
    }
    return result;
}
}),
"[project]/src/lib/webhook.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

return __turbopack_context__.a(async (__turbopack_handle_async_dependencies__, __turbopack_async_result__) => { try {

__turbopack_context__.s([
    "dispatchWebhook",
    ()=>dispatchWebhook,
    "downloadAndSaveMedia",
    ()=>downloadAndSaveMedia,
    "onConnectionUpdate",
    ()=>onConnectionUpdate,
    "onMessageReceived",
    ()=>onMessageReceived,
    "onMessageSent",
    ()=>onMessageSent
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/prisma.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$crypto__$5b$external$5d$__$28$crypto$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/crypto [external] (crypto, cjs)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/@whiskeysockets/baileys/lib/index.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$messages$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@whiskeysockets/baileys/lib/Utils/messages.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$fs$2f$promises__$5b$external$5d$__$28$fs$2f$promises$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/fs/promises [external] (fs/promises, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/path [external] (path, cjs)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$jid$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/jid-utils.ts [app-route] (ecmascript)");
var __turbopack_async_dependencies__ = __turbopack_handle_async_dependencies__([
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__,
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$messages$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__
]);
[__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$messages$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__] = __turbopack_async_dependencies__.then ? (await __turbopack_async_dependencies__)() : __turbopack_async_dependencies__;
;
;
;
;
;
;
async function dispatchWebhook(sessionId, event, data) {
    try {
        // Get the session to find the userId
        const session = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findUnique({
            where: {
                sessionId
            },
            select: {
                id: true,
                userId: true
            }
        });
        if (!session) {
            console.warn(`Webhook dispatch: Session ${sessionId} not found`);
            return;
        }
        // Find all active webhooks for this user/session
        const webhooks = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].webhook.findMany({
            where: {
                userId: session.userId,
                isActive: true,
                OR: [
                    {
                        sessionId: null
                    },
                    {
                        sessionId: session.id
                    } // Session-specific webhooks
                ]
            }
        });
        if (webhooks.length === 0) return;
        const payload = {
            event,
            sessionId,
            timestamp: new Date().toISOString(),
            data: normalizePayloadData(event, data) // Normalize data before sending
        };
        // Dispatch to all matching webhooks
        for (const webhook of webhooks){
            // Check if this webhook subscribes to this event
            const events = webhook.events || [];
            if (!events.includes(event) && !events.includes("*")) {
                continue;
            }
            // Send webhook in background
            sendWebhookRequest(webhook.url, payload, webhook.secret).catch((err)=>{
                console.error(`Webhook ${webhook.id} failed:`, err);
            });
        }
    } catch (error) {
        console.error("Webhook dispatch error:", error);
    }
}
/**
 * Normalize payload data to match API format and avoid Circular/BigInt errors
 */ function normalizePayloadData(event, data) {
    if (event === "message.received" || event === "message.sent") {
        // If data is already simplified, return it
        if (data.type && data.content) return data;
        // If data is raw Baileys message (which we shouldn't be passing raw anymore, but just in case)
        // Ideally the caller (onMessageReceived) should have already simplified it.
        // But let's handle the specific fields passed by onMessageReceived below.
        return data;
    }
    return data;
}
/**
 * JSON Replacer to handle BigInt
 */ function jsonReplacer(key, value) {
    if (typeof value === 'bigint') {
        return value.toString();
    }
    return value;
}
/**
 * Send HTTP POST request to webhook endpoint
 */ async function sendWebhookRequest(url, payload, secret) {
    // Use custom replacer for BigInt support
    const body = JSON.stringify(payload, jsonReplacer);
    const headers = {
        "Content-Type": "application/json",
        "User-Agent": "WA-AB-Webhook/1.0"
    };
    // Add HMAC signature if secret is provided
    if (secret) {
        const signature = __TURBOPACK__imported__module__$5b$externals$5d2f$crypto__$5b$external$5d$__$28$crypto$2c$__cjs$29$__["default"].createHmac("sha256", secret).update(body).digest("hex");
        headers["X-Webhook-Signature"] = `sha256=${signature}`;
    }
    const response = await fetch(url, {
        method: "POST",
        headers,
        body,
        signal: AbortSignal.timeout(10000) // 10 second timeout
    });
    if (!response.ok) {
        throw new Error(`Webhook returned ${response.status}: ${response.statusText}`);
    }
    return response;
}
async function downloadAndSaveMedia(message, sessionId) {
    try {
        const messageContent = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$messages$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["normalizeMessageContent"])(message.message);
        if (!messageContent) {
            console.log("MediaDownload: No content normalized");
            return null;
        }
        const messageType = Object.keys(messageContent)[0];
        console.log(`MediaDownload: Types checking... Found: ${messageType}`);
        if (![
            'imageMessage',
            'videoMessage',
            'audioMessage',
            'documentMessage',
            'stickerMessage'
        ].includes(messageType)) {
            console.log(`MediaDownload: Message type ${messageType} is not a downloadable media.`);
            return null;
        }
        console.log(`MediaDownload: Attempting to download ${messageType}...`);
        const buffer = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$messages$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["downloadMediaMessage"])(message, "buffer", {});
        if (!buffer) {
            console.log("MediaDownload: Buffer is empty/null");
            return null;
        }
        console.log(`MediaDownload: Downloaded ${buffer.length} bytes.`);
        // Generate filename
        const extMap = {
            imageMessage: 'jpg',
            videoMessage: 'mp4',
            audioMessage: 'mp3',
            documentMessage: 'bin',
            stickerMessage: 'webp'
        };
        let ext = extMap[messageType] || 'bin';
        // Try to get extension from mimetype if available
        const mime = messageContent[messageType]?.mimetype;
        if (mime) {
            const mimeExt = mime.split('/')[1]?.split(';')[0];
            if (mimeExt) ext = mimeExt;
        }
        const filename = `${sessionId}-${message.key.id}.${ext}`;
        const filePath = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(process.cwd(), "data", "media", filename);
        console.log(`MediaDownload: Saving to ${filePath}`);
        // Ensure directory exists (redundant if handled by OS, but safe)
        await (0, __TURBOPACK__imported__module__$5b$externals$5d2f$fs$2f$promises__$5b$external$5d$__$28$fs$2f$promises$2c$__cjs$29$__["mkdir"])(__TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].dirname(filePath), {
            recursive: true
        });
        await (0, __TURBOPACK__imported__module__$5b$externals$5d2f$fs$2f$promises__$5b$external$5d$__$28$fs$2f$promises$2c$__cjs$29$__["writeFile"])(filePath, buffer);
        // Return URL path using API route for reliable serving
        const fileUrl = `/api/media/${filename}`;
        console.log(`MediaDownload: Success. URL: ${fileUrl}`);
        return fileUrl;
    } catch (e) {
        console.error("Failed to download media:", e);
        return null;
    }
}
async function onMessageReceived(sessionId, message, existingFileUrl) {
    // Re-calculate fields to match store logic EXACTLY
    const remoteJid = message.key?.remoteJid || "";
    const fromMe = message.key?.fromMe || false;
    const isGroup = remoteJid.endsWith("@g.us");
    const participant = isGroup ? message.key?.participant || message.participant : undefined;
    // Extract Alt JID (e.g. Phone Number JID when remoteJid is LID)
    const remoteJidAlt = message.key?.remoteJidAlt || null;
    // --- Consistent JID Normalization ---
    // Always prefer @s.whatsapp.net format over @lid
    const normalizedFrom = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$jid$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["resolveToPhoneJidBySessionId"])(remoteJid, sessionId, remoteJidAlt);
    // "sender" is who sent it. In DM: remoteJid. In Group: participant.
    let senderJid = isGroup ? participant || "" : remoteJid;
    const normalizedSender = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$jid$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["resolveToPhoneJidBySessionId"])(senderJid, sessionId);
    let sender = normalizedSender;
    // Enrich Participant Data if Group
    let participantDetail = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$jid$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["resolveToPhoneJidBySessionId"])(participant || "", sessionId);
    if (isGroup && typeof sender === 'string') {
        try {
            const session = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findUnique({
                where: {
                    sessionId
                },
                select: {
                    id: true
                }
            });
            if (session) {
                const group = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].group.findUnique({
                    where: {
                        sessionId_jid: {
                            sessionId: session.id,
                            jid: remoteJid
                        }
                    },
                    select: {
                        participants: true
                    }
                });
                if (group && group.participants) {
                    const parts = group.participants;
                    const found = parts.find((p)=>p.id === senderJid || p.id === participant || p.id === normalizedSender);
                    if (found) {
                        sender = found;
                        participantDetail = found;
                    }
                }
            }
        } catch (e) {
            console.error("Failed to enrich participant", e);
        }
    }
    // Download media if available (or use existing)
    let fileUrl = existingFileUrl || null;
    if (!fileUrl) {
        try {
            fileUrl = await downloadAndSaveMedia(message, sessionId);
        } catch (e) {
            console.error("Error handling media download", e);
        }
    }
    const normalized = extractMessageContent(message);
    const quoted = await extractQuotedMessageAsync(message, sessionId);
    dispatchWebhook(sessionId, "message.received", {
        key: {
            id: message.key?.id,
            remoteJid: normalizedFrom,
            fromMe: fromMe,
            participant: participantDetail
        },
        pushName: message.pushName,
        messageTimestamp: message.messageTimestamp,
        // Simplified Fields — always @s.whatsapp.net format
        from: normalizedFrom,
        sender: sender,
        isGroup: isGroup,
        chatType: getChatType(remoteJid),
        // Message Content
        type: normalized.type,
        content: normalized.content,
        fileUrl: fileUrl,
        caption: normalized.caption,
        quoted: quoted,
        // Raw Data
        raw: message
    });
}
async function onMessageSent(sessionId, message, existingFileUrl) {
    const normalized = extractMessageContent(message);
    const quoted = await extractQuotedMessageAsync(message, sessionId);
    const remoteJid = message.key?.remoteJid || "";
    const isGroup = remoteJid.endsWith("@g.us");
    const remoteJidAlt = message.key?.remoteJidAlt || null;
    // Download media for sent messages too
    let fileUrl = existingFileUrl || null;
    if (!fileUrl) {
        try {
            fileUrl = await downloadAndSaveMedia(message, sessionId);
        } catch (e) {}
    }
    // --- Consistent JID Normalization (same as onMessageReceived) ---
    const normalizedFrom = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$jid$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["resolveToPhoneJidBySessionId"])(remoteJid, sessionId, remoteJidAlt);
    // For sent messages, sender is "ME" (self)
    // But we still normalize the participant JID if in a group
    const rawSender = message.key?.participant || (message.key?.fromMe ? "ME" : remoteJid);
    const sender = rawSender === "ME" ? "ME" : await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$jid$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["resolveToPhoneJidBySessionId"])(rawSender, sessionId);
    dispatchWebhook(sessionId, "message.sent", {
        key: {
            id: message.key?.id,
            remoteJid: normalizedFrom,
            fromMe: message.key?.fromMe || true,
            participant: isGroup ? sender : undefined
        },
        // Simplified Fields — always @s.whatsapp.net format
        from: normalizedFrom,
        sender: sender,
        isGroup: isGroup,
        chatType: getChatType(remoteJid),
        type: normalized.type,
        content: normalized.content,
        fileUrl: fileUrl,
        caption: normalized.caption,
        quoted: quoted,
        timestamp: Date.now(),
        raw: message
    });
}
/**
 * Determine chat type from JID
 */ function getChatType(jid) {
    if (!jid) return "UNKNOWN";
    if (jid.endsWith("@g.us")) return "GROUP";
    if (jid.endsWith("@s.whatsapp.net")) return "PERSONAL";
    if (jid.endsWith("@lid")) return "PERSONAL"; // LID is also a personal chat
    if (jid === "status@broadcast") return "STATUS";
    if (jid.endsWith("@newsletter")) return "NEWSLETTER";
    return "UNKNOWN";
}
function onConnectionUpdate(sessionId, status, qr) {
    dispatchWebhook(sessionId, "connection.update", {
        status,
        qr: qr || null
    });
}
/**
 * Extract content and type from Baileys message
 */ function extractMessageContent(msg) {
    const messageContent = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$messages$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["normalizeMessageContent"])(msg.message);
    let text = "";
    let caption = undefined;
    let messageType = "TEXT";
    if (!messageContent) return {
        type: "UNKNOWN",
        content: ""
    };
    if (messageContent.conversation) {
        text = messageContent.conversation;
    } else if (messageContent.extendedTextMessage?.text) {
        text = messageContent.extendedTextMessage.text;
    } else if (messageContent.imageMessage) {
        messageType = "IMAGE";
        caption = messageContent.imageMessage.caption || "";
        text = caption; // Content often used as text display
    } else if (messageContent.videoMessage) {
        messageType = "VIDEO";
        caption = messageContent.videoMessage.caption || "";
        text = caption;
    } else if (messageContent.audioMessage) {
        messageType = "AUDIO";
    } else if (messageContent.documentMessage) {
        messageType = "DOCUMENT";
        text = messageContent.documentMessage.fileName || "";
        caption = messageContent.documentMessage.caption || "";
    } else if (messageContent.stickerMessage) {
        messageType = "STICKER";
    } else if (messageContent.locationMessage) {
        messageType = "LOCATION";
        text = `${messageContent.locationMessage.degreesLatitude},${messageContent.locationMessage.degreesLongitude}`;
    } else if (messageContent.contactMessage) {
        messageType = "CONTACT";
        text = messageContent.contactMessage.displayName || "";
    }
    return {
        type: messageType,
        content: text,
        caption
    };
}
/**
 * Extract Quoted Message recursively (Async to Lookup DB)
 */ async function extractQuotedMessageAsync(msg, sessionId) {
    const messageContent = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$messages$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["normalizeMessageContent"])(msg.message);
    if (!messageContent) return null;
    // Check for contextInfo in common message types
    let contextInfo = null;
    if (messageContent.extendedTextMessage) {
        contextInfo = messageContent.extendedTextMessage.contextInfo;
    } else if (messageContent.imageMessage) {
        contextInfo = messageContent.imageMessage.contextInfo;
    } else if (messageContent.videoMessage) {
        contextInfo = messageContent.videoMessage.contextInfo;
    } else if (messageContent.audioMessage) {
        contextInfo = messageContent.audioMessage.contextInfo;
    } else if (messageContent.stickerMessage) {
        contextInfo = messageContent.stickerMessage.contextInfo;
    } else if (messageContent.documentMessage) {
        contextInfo = messageContent.documentMessage.contextInfo;
    } else if (messageContent.contactMessage) {
        contextInfo = messageContent.contactMessage.contextInfo;
    } else if (messageContent.locationMessage) {
        contextInfo = messageContent.locationMessage.contextInfo;
    }
    if (contextInfo && contextInfo.quotedMessage) {
        const quotedMsg = contextInfo.quotedMessage;
        const normalized = extractMessageContent({
            message: quotedMsg
        });
        let fileUrl = null;
        // Lookup Media URL in DB if possible
        if (contextInfo.stanzaId) {
            try {
                // We need the dbSessionId... this is tricky without fetching session again.
                // But we can try to look up by sessionId (baileys ID) and keyId
                // Message table has @@unique([sessionId, keyId]). BUT sessionId there is the CUID, not the string.
                // Fetch CUID First
                const session = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findUnique({
                    where: {
                        sessionId
                    },
                    select: {
                        id: true
                    }
                });
                if (session) {
                    const savedMsg = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].message.findUnique({
                        where: {
                            sessionId_keyId: {
                                sessionId: session.id,
                                keyId: contextInfo.stanzaId
                            }
                        },
                        select: {
                            mediaUrl: true
                        }
                    });
                    if (savedMsg?.mediaUrl) {
                        fileUrl = savedMsg.mediaUrl;
                    }
                }
            } catch (e) {
                console.error("Failed to lookup quoted media url", e);
            }
        }
        return {
            key: {
                remoteJid: contextInfo.remoteJid || null,
                participant: contextInfo.participant || null,
                fromMe: contextInfo.participant === undefined,
                id: contextInfo.stanzaId || null
            },
            type: normalized.type,
            content: normalized.content,
            caption: normalized.caption,
            fileUrl: fileUrl
        };
    }
    return null;
}
__turbopack_async_result__();
} catch(e) { __turbopack_async_result__(e); } }, false);}),
"[externals]/constants [external] (constants, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("constants", () => require("constants"));

module.exports = mod;
}),
"[externals]/tty [external] (tty, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("tty", () => require("tty"));

module.exports = mod;
}),
"[project]/src/modules/whatsapp/bot/command-handler.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

return __turbopack_context__.a(async (__turbopack_handle_async_dependencies__, __turbopack_async_result__) => { try {

__turbopack_context__.s([
    "handleBotCommand",
    ()=>handleBotCommand,
    "setSessionStartTime",
    ()=>setSessionStartTime
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/prisma.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/@whiskeysockets/baileys/lib/index.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$messages$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@whiskeysockets/baileys/lib/Utils/messages.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$wa$2d$sticker$2d$formatter$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/wa-sticker-formatter/dist/index.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$sharp__$5b$external$5d$__$28$sharp$2c$__cjs$2c$__$5b$project$5d2f$node_modules$2f$sharp$29$__ = __turbopack_context__.i("[externals]/sharp [external] (sharp, cjs, [project]/node_modules/sharp)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$fs$2f$promises__$5b$external$5d$__$28$fs$2f$promises$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/fs/promises [external] (fs/promises, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/path [external] (path, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$os__$5b$external$5d$__$28$os$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/os [external] (os, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$child_process__$5b$external$5d$__$28$child_process$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/child_process [external] (child_process, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$util__$5b$external$5d$__$28$util$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/util [external] (util, cjs)");
var __turbopack_async_dependencies__ = __turbopack_handle_async_dependencies__([
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__,
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$messages$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__
]);
[__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$messages$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__] = __turbopack_async_dependencies__.then ? (await __turbopack_async_dependencies__)() : __turbopack_async_dependencies__;
;
;
;
;
;
;
;
;
;
const execAsync = (0, __TURBOPACK__imported__module__$5b$externals$5d2f$util__$5b$external$5d$__$28$util$2c$__cjs$29$__["promisify"])(__TURBOPACK__imported__module__$5b$externals$5d2f$child_process__$5b$external$5d$__$28$child_process$2c$__cjs$29$__["exec"]);
// Map to track start times for uptime
const startTimes = new Map();
// Default bot config
// Default bot config
const DEFAULT_CONFIG = {
    enabled: true,
    botMode: 'OWNER',
    botAllowedJids: [],
    autoReplyMode: 'ALL',
    autoReplyAllowedJids: [],
    enableSticker: true,
    enableVideoSticker: true,
    maxStickerDuration: 10,
    enablePing: true,
    enableUptime: true,
    botName: "WA-AB Bot",
    prefix: "#",
    removeBgApiKey: null
};
function setSessionStartTime(sessionId) {
    if (!startTimes.has(sessionId)) {
        startTimes.set(sessionId, Date.now());
    }
}
async function handleBotCommand(sock, sessionId, msg) {
    if (!sock || !msg.message || !msg.key.remoteJid) return;
    const remoteJid = msg.key.remoteJid;
    const fromMe = msg.key.fromMe || false;
    // Get text content
    let text = "";
    const messageContent = msg.message;
    if (messageContent.conversation) {
        text = messageContent.conversation;
    } else if (messageContent.extendedTextMessage?.text) {
        text = messageContent.extendedTextMessage.text;
    } else if (messageContent.imageMessage?.caption) {
        text = messageContent.imageMessage.caption;
    } else if (messageContent.videoMessage?.caption) {
        text = messageContent.videoMessage.caption;
    }
    // Quick check: skip non-command messages early (common prefixes)
    // We'll do a proper prefix check after loading config
    if (!text || text.length === 0) return;
    // Fetch session first
    const session = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findUnique({
        where: {
            sessionId
        },
        select: {
            id: true
        }
    });
    if (!session) return;
    // Fetch BotConfig separately
    // @ts-ignore - Prisma Client types might lag in IDE
    const botConfig = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].botConfig.findUnique({
        where: {
            sessionId: session.id
        }
    });
    const config = botConfig || DEFAULT_CONFIG;
    if (!config.enabled) return;
    // Now check prefix with loaded config
    const prefix = config.prefix || "#";
    if (!text.startsWith(prefix)) return;
    // Verify Access Permissions
    const botMode = config.botMode || 'OWNER'; // Default to OWNER if missing
    // Check Permission
    let canExecute = false;
    if (fromMe) {
        canExecute = true; // Owner always allowed
    } else {
        if (botMode === 'ALL') {
            canExecute = true;
        } else if (botMode === 'SPECIFIC') {
            const allowedJids = config.botAllowedJids || [];
            // Standardized Sender Logic (matches webhook & store)
            const isGroup = msg.key.remoteJid?.endsWith("@g.us") || false;
            const remoteJidAlt = msg.key.remoteJidAlt;
            let senderJid = (isGroup ? msg.key.participant || msg.participant : msg.key.remoteJid) || "";
            if (!isGroup && remoteJidAlt) {
                senderJid = remoteJidAlt;
            }
            if (Array.isArray(allowedJids)) {
                canExecute = allowedJids.some((jid)=>senderJid.includes(jid));
            }
        } else if (botMode === 'BLACKLIST') {
            const blockedJids = config.botBlockedJids || [];
            const isGroup = msg.key.remoteJid?.endsWith("@g.us") || false;
            const remoteJidAlt = msg.key.remoteJidAlt;
            let senderJid = (isGroup ? msg.key.participant || msg.participant : msg.key.remoteJid) || "";
            if (!isGroup && remoteJidAlt) {
                senderJid = remoteJidAlt;
            }
            // If blacklist, allowed by default UNLESS in blocked list
            canExecute = true;
            if (Array.isArray(blockedJids)) {
                const isBlocked = blockedJids.some((jid)=>senderJid.includes(jid));
                if (isBlocked) canExecute = false;
            }
        }
    }
    if (!canExecute) return;
    const [command, ...args] = text.trim().split(" ");
    const cmd = command.toLowerCase().slice(prefix.length); // remove prefix
    try {
        switch(cmd){
            case "ping":
                {
                    if (!config.enablePing) return;
                    await sock.sendMessage(remoteJid, {
                        text: "Pong! 🏓"
                    }, {
                        quoted: msg
                    });
                    break;
                }
            case "id":
                {
                    await sock.sendMessage(remoteJid, {
                        text: `*Chat ID:* \`${remoteJid}\``
                    }, {
                        quoted: msg
                    });
                    break;
                }
            case "uptime":
                {
                    if (!config.enableUptime) return;
                    const start = startTimes.get(sessionId) || Date.now();
                    const uptimeMs = Date.now() - start;
                    const hours = Math.floor(uptimeMs / 3600000);
                    const minutes = Math.floor(uptimeMs % 3600000 / 60000);
                    const seconds = Math.floor(uptimeMs % 60000 / 1000);
                    await sock.sendMessage(remoteJid, {
                        text: `*Session Uptime:* ${hours}h ${minutes}m ${seconds}s`
                    }, {
                        quoted: msg
                    });
                    break;
                }
            case "sticker":
            case "s":
            case "stiker":
                {
                    if (!config.enableSticker) return;
                    // Check if message has image or video
                    let mediaMsg = msg;
                    // If quoted, check quoted
                    const quoted = messageContent.extendedTextMessage?.contextInfo?.quotedMessage;
                    if (quoted) {
                        mediaMsg = {
                            key: {
                                remoteJid,
                                id: messageContent.extendedTextMessage?.contextInfo?.stanzaId
                            },
                            message: quoted
                        };
                    }
                    const msgContent = mediaMsg.message;
                    const isImage = !!msgContent?.imageMessage;
                    const isVideo = !!msgContent?.videoMessage;
                    if (!isImage && !isVideo) {
                        await sock.sendMessage(remoteJid, {
                            text: "❌ Please reply to an image/video or send media with caption #sticker"
                        }, {
                            quoted: msg
                        });
                        return;
                    }
                    if (msgContent?.extendedTextMessage) {
                        await sock.sendMessage(remoteJid, {
                            text: "❌ Cannot convert text message to sticker."
                        }, {
                            quoted: msg
                        });
                        return;
                    }
                    // Handle Video Limits
                    if (isVideo) {
                        if (!config.enableVideoSticker) {
                            await sock.sendMessage(remoteJid, {
                                text: "❌ Video stickers are disabled in bot settings."
                            }, {
                                quoted: msg
                            });
                            return;
                        }
                        const seconds = msgContent?.videoMessage?.seconds || 0;
                        const maxDuration = config.maxStickerDuration || 10;
                        if (seconds > maxDuration) {
                            await sock.sendMessage(remoteJid, {
                                text: `❌ Video too long! Max duration is ${maxDuration} seconds.`
                            }, {
                                quoted: msg
                            });
                            return;
                        }
                    }
                    await sock.sendMessage(remoteJid, {
                        react: {
                            text: "⏳",
                            key: msg.key
                        }
                    });
                    try {
                        // Download
                        let buffer = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$messages$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["downloadMediaMessage"])(mediaMsg, "buffer", {}, {
                            logger: console,
                            reuploadRequest: sock.updateMediaMessage
                        });
                        // Resize/Compress Logic
                        if (isImage) {
                            try {
                                // Use limitInputPixels: false to handle large images
                                buffer = await (0, __TURBOPACK__imported__module__$5b$externals$5d2f$sharp__$5b$external$5d$__$28$sharp$2c$__cjs$2c$__$5b$project$5d2f$node_modules$2f$sharp$29$__["default"])(buffer, {
                                    limitInputPixels: false
                                }).resize(512, 512, {
                                    fit: 'inside',
                                    withoutEnlargement: true
                                }).toBuffer();
                            } catch (resizeErr) {
                                console.error("Image Resize failed", resizeErr);
                            }
                        } else if (isVideo) {
                            try {
                                const tempInput = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(__TURBOPACK__imported__module__$5b$externals$5d2f$os__$5b$external$5d$__$28$os$2c$__cjs$29$__["default"].tmpdir(), `input_${Date.now()}.mp4`);
                                const tempOutput = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(__TURBOPACK__imported__module__$5b$externals$5d2f$os__$5b$external$5d$__$28$os$2c$__cjs$29$__["default"].tmpdir(), `output_${Date.now()}.mp4`);
                                await __TURBOPACK__imported__module__$5b$externals$5d2f$fs$2f$promises__$5b$external$5d$__$28$fs$2f$promises$2c$__cjs$29$__["default"].writeFile(tempInput, buffer);
                                // Compress Video using ffmpeg
                                // Extreme Compression: 8fps, CRF 40, 300k bitrate, ultrafast
                                await execAsync(`ffmpeg -y -i "${tempInput}" -vf "scale=512:512:force_original_aspect_ratio=decrease,fps=10" -c:v libx264 -preset ultrafast -crf 40 -b:v 300k -maxrate 300k -bufsize 600k -an "${tempOutput}"`);
                                buffer = await __TURBOPACK__imported__module__$5b$externals$5d2f$fs$2f$promises__$5b$external$5d$__$28$fs$2f$promises$2c$__cjs$29$__["default"].readFile(tempOutput);
                                // Cleanup
                                await __TURBOPACK__imported__module__$5b$externals$5d2f$fs$2f$promises__$5b$external$5d$__$28$fs$2f$promises$2c$__cjs$29$__["default"].unlink(tempInput).catch(()=>{});
                                await __TURBOPACK__imported__module__$5b$externals$5d2f$fs$2f$promises__$5b$external$5d$__$28$fs$2f$promises$2c$__cjs$29$__["default"].unlink(tempOutput).catch(()=>{});
                            } catch (videoErr) {
                                console.error("Video Compression failed", videoErr);
                            // Continue with original buffer if compression fails, or throw? 
                            // If it fails, likely original will fail too, but let's try.
                            }
                        }
                        // Check for background removal (Only for Images)
                        const isRemoveBg = args.includes("nobg") || args.includes("removebg");
                        if (isImage && isRemoveBg && config.removeBgApiKey) {
                            try {
                                // Convert Buffer to Uint8Array for Blob compatibility
                                const uint8Array = new Uint8Array(buffer);
                                const blob = new Blob([
                                    uint8Array
                                ], {
                                    type: 'image/png'
                                });
                                const formData = new FormData();
                                formData.append('image_file', blob, 'image.png');
                                formData.append('size', 'auto');
                                const res = await fetch('https://api.remove.bg/v1.0/removebg', {
                                    method: 'POST',
                                    headers: {
                                        'X-Api-Key': config.removeBgApiKey
                                    },
                                    body: formData
                                });
                                if (res.ok) {
                                    const arrayBuffer = await res.arrayBuffer();
                                    buffer = Buffer.from(arrayBuffer);
                                } else {
                                    const err = await res.json();
                                    throw new Error(`RemoveBG Error: ${err.errors?.[0]?.title || res.statusText}`);
                                }
                            } catch (bgError) {
                                console.error("RemoveBG Failed:", bgError);
                                await sock.sendMessage(remoteJid, {
                                    text: `⚠️ Remove BG failed: ${bgError.message}. Sending normal sticker...`
                                }, {
                                    quoted: msg
                                });
                            }
                        } else if (isImage && isRemoveBg && !config.removeBgApiKey) {
                            await sock.sendMessage(remoteJid, {
                                text: `⚠️ Remove BG API Key not configured in dashboard. Sending normal sticker...`
                            }, {
                                quoted: msg
                            });
                        }
                        // Convert
                        const sticker = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$wa$2d$sticker$2d$formatter$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"](buffer, {
                            pack: config.botName || "WA-AB Bot",
                            author: "By " + (config.botName || "WA-AB Bot"),
                            type: "full",
                            quality: 15 // Extreme quality reduction for size
                        });
                        const stickerBuffer = await sticker.toBuffer();
                        // Send
                        await sock.sendMessage(remoteJid, {
                            sticker: stickerBuffer
                        }, {
                            quoted: msg
                        });
                        await sock.sendMessage(remoteJid, {
                            react: {
                                text: "✅",
                                key: msg.key
                            }
                        });
                    } catch (e) {
                        console.error("Sticker generation failed", e);
                        await sock.sendMessage(remoteJid, {
                            text: "❌ Failed to create sticker. Error: " + e.message
                        }, {
                            quoted: msg
                        });
                    }
                    break;
                }
            case "menu":
            case "help":
                {
                    const botName = config.botName || "WA-AB Bot";
                    const menu = `
🤖 *${botName} Menu* 🤖

📌 *Commands:*
• *${prefix}sticker* / *${prefix}s*: Convert Image/Video to Sticker
  - Supports Images, GIFs, and Videos (max ${config.maxStickerDuration || 10}s)
  - Use *${prefix}sticker nobg* to remove background (Images only)
• *${prefix}ping*: Check Bot Status
• *${prefix}uptime*: Check Session Uptime
• *${prefix}id*: Get Chat ID

_Made with ❤️_
`;
                    await sock.sendMessage(remoteJid, {
                        text: menu
                    }, {
                        quoted: msg
                    });
                    break;
                }
            default:
                break;
        }
    } catch (e) {
        console.error("Bot command error", e);
    }
}
__turbopack_async_result__();
} catch(e) { __turbopack_async_result__(e); } }, false);}),
"[project]/src/modules/whatsapp/store/index.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

return __turbopack_context__.a(async (__turbopack_handle_async_dependencies__, __turbopack_async_result__) => { try {

__turbopack_context__.s([
    "bindSessionStore",
    ()=>bindSessionStore
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/prisma.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/@whiskeysockets/baileys/lib/index.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$messages$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@whiskeysockets/baileys/lib/Utils/messages.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$webhook$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/webhook.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$bot$2f$command$2d$handler$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/modules/whatsapp/bot/command-handler.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$jid$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/jid-utils.ts [app-route] (ecmascript)");
var __turbopack_async_dependencies__ = __turbopack_handle_async_dependencies__([
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__,
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$messages$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__,
    __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$webhook$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__,
    __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$bot$2f$command$2d$handler$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__
]);
[__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$messages$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$webhook$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$bot$2f$command$2d$handler$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__] = __turbopack_async_dependencies__.then ? (await __turbopack_async_dependencies__)() : __turbopack_async_dependencies__;
;
;
;
;
;
const bindSessionStore = (sock, sessionId, io)=>{
    // Set start time for uptime command
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$bot$2f$command$2d$handler$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["setSessionStartTime"])(sessionId);
    // First, get the database Session ID (cuid)
    let dbSessionId = null;
    // Initialize by fetching the session ID
    (async ()=>{
        const session = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findUnique({
            where: {
                sessionId
            },
            select: {
                id: true
            }
        });
        if (session) {
            dbSessionId = session.id;
            console.log(`Message store initialized for session ${sessionId} (db: ${dbSessionId})`);
        } else {
            console.error(`Session ${sessionId} not found for message store`);
        }
    })();
    // Handle Messages
    sock.ev.on('messages.upsert', async ({ messages, type })=>{
        // Process all message types: notify, append, and history sync
        if (type !== 'notify' && type !== 'append') {
            // For history sync, we still want to save messages
            console.log(`Received ${messages.length} messages of type: ${type}`);
        }
        // Emit to socket room for real-time frontend updates
        if (type === 'notify' || type === 'append') {
            io?.to(sessionId).emit('message.upsert', {
                messages,
                type
            });
        }
        // Ensure we have the database session ID
        if (!dbSessionId) {
            const session = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findUnique({
                where: {
                    sessionId
                },
                select: {
                    id: true
                }
            });
            if (!session) return;
            dbSessionId = session.id;
        }
        const processedMessages = [];
        // Fetch Bot Config for auto-read & welcome message
        const config = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].botConfig.findUnique({
            where: {
                sessionId: dbSessionId
            }
        });
        for (const msg of messages){
            try {
                // Auto Read Logic
                if (type === 'notify' && config?.autoRead && !msg.key.fromMe) {
                    await sock.readMessages([
                        msg.key
                    ]);
                }
                const savedMessage = await processAndSaveMessage(msg, dbSessionId, sessionId, type === 'notify', sock, config);
                if (savedMessage) {
                    processedMessages.push(savedMessage);
                }
                // Execute Bot Commands (Only for Notify / New Messages)
                if (type === 'notify' && savedMessage) {
                    // Run in background, don't await strictly to not block saving
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$bot$2f$command$2d$handler$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["handleBotCommand"])(sock, sessionId, msg).catch((e)=>console.error("Bot Handler Error", e));
                }
            } catch (error) {
                console.error("Error saving message", error);
            }
        }
        // Emit to socket room for real-time frontend updates
        if (processedMessages.length > 0) {
            // Serialize for frontend: convert Date to ISO string
            const serialized = processedMessages.map((m)=>({
                    ...m,
                    timestamp: m.timestamp instanceof Date ? m.timestamp.toISOString() : m.timestamp
                }));
            console.log(`[Socket] Emitting message.update for ${serialized.length} messages in session ${sessionId}`, serialized.map((m)=>({
                    keyId: m.keyId,
                    remoteJid: m.remoteJid,
                    fromMe: m.fromMe
                })));
            io?.to(sessionId).emit('message.update', serialized);
        }
    });
    // Handle Message History Sync (when connecting for the first time or syncing)
    sock.ev.on('messaging-history.set', async ({ messages, chats, contacts, isLatest })=>{
        console.log(`History sync: ${messages?.length || 0} messages, ${chats?.length || 0} chats, ${contacts?.length || 0} contacts, latest: ${isLatest}`);
        // Ensure we have the database session ID
        if (!dbSessionId) {
            const session = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findUnique({
                where: {
                    sessionId
                },
                select: {
                    id: true
                }
            });
            if (!session) return;
            dbSessionId = session.id;
        }
        // Save all historical messages
        if (messages && messages.length > 0) {
            console.log(`Syncing ${messages.length} historical messages...`);
            for (const msg of messages){
                try {
                    await processAndSaveMessage(msg, dbSessionId, sessionId, false, sock);
                } catch (error) {
                    console.error("Error saving historical message", error);
                }
            }
            console.log(`Finished syncing ${messages.length} historical messages`);
        }
        // Note: Contacts and Chats are synced by src/modules/whatsapp/store/contacts.ts
        // We only handle messages here to avoid P2002 Unique Constraint Race Conditions.
        console.log(`Finished syncing ${messages.length} historical messages`);
    });
    // Handle Contacts Upsert
    sock.ev.on('contacts.upsert', async (contacts)=>{
        // Ensure we have the database session ID
        if (!dbSessionId) {
            const session = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findUnique({
                where: {
                    sessionId
                },
                select: {
                    id: true
                }
            });
            if (!session) return;
            dbSessionId = session.id;
        }
        for (const c of contacts){
            try {
                if (!c.id) continue;
                await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].contact.upsert({
                    where: {
                        sessionId_jid: {
                            sessionId: dbSessionId,
                            jid: c.id
                        }
                    },
                    create: {
                        sessionId: dbSessionId,
                        jid: c.id,
                        // @ts-ignore
                        lid: c.lid || undefined,
                        name: c.name || c.notify || c.verifiedName,
                        notify: c.notify,
                        // @ts-ignore
                        verifiedName: c.verifiedName,
                        profilePic: c.imgUrl || undefined,
                        data: c
                    },
                    update: {
                        // @ts-ignore
                        lid: c.lid || undefined,
                        name: c.name || undefined,
                        notify: c.notify || undefined,
                        // @ts-ignore
                        verifiedName: c.verifiedName || undefined,
                        profilePic: c.imgUrl || undefined,
                        data: c
                    }
                });
                // Dispatch webhook for contact update
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$webhook$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["dispatchWebhook"])(sessionId, "contact.update", {
                    jid: c.id,
                    name: c.name,
                    notify: c.notify
                });
            } catch (e) {
                console.error("Error saving contact", e);
            }
        }
    });
    // Handle Message Status Updates
    sock.ev.on('messages.update', async (updates)=>{
        if (!dbSessionId) return;
        for (const update of updates){
            try {
                const keyId = update.key?.id;
                if (!keyId) continue;
                const statusMap = {
                    0: 'PENDING',
                    1: 'SENT',
                    2: 'DELIVERED',
                    3: 'READ',
                    4: 'READ'
                };
                const status = statusMap[update.update?.status || 0] || 'PENDING';
                await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].message.updateMany({
                    where: {
                        sessionId: dbSessionId,
                        keyId
                    },
                    data: {
                        status: status
                    }
                });
                // Dispatch webhook for message status update
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$webhook$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["dispatchWebhook"])(sessionId, "message.status", {
                    keyId,
                    remoteJid: update.key?.remoteJid,
                    status
                });
            } catch (e) {
                console.error("Error updating message status", e);
            }
        }
    });
};
async function processAndSaveMessage(msg, dbSessionId, sessionId, triggerWebhook, sock, config) {
    const keyId = msg.key.id;
    const remoteJid = msg.key.remoteJid;
    const fromMe = msg.key.fromMe;
    const pushName = msg.pushName;
    const timestamp = msg.messageTimestamp ? new Date((typeof msg.messageTimestamp === 'number' ? msg.messageTimestamp : Number(msg.messageTimestamp)) * 1000) : new Date();
    // Filter out Protocol & Empty Messages
    if (!msg.message) return false;
    if (!keyId || !remoteJid) return false;
    // Ignore specific technical message types
    const messageKeys = Object.keys(msg.message);
    const ignoredTypes = [
        'protocolMessage',
        'senderKeyDistributionMessage',
        'reactionMessage',
        'keepInChatMessage'
    ];
    // If message only contains ignored types, skip
    if (messageKeys.every((k)=>ignoredTypes.includes(k))) {
        console.log(`Skipping technical message: ${keyId} (${messageKeys.join(', ')})`);
        return null;
    }
    // Check if message already exists to avoid duplicates
    // Baileys 'notify' event can sometimes trigger multiple times or for history
    // Baileys 'notify' event can sometimes trigger multiple times or for history
    const existingMessage = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].message.findUnique({
        where: {
            sessionId_keyId: {
                sessionId: dbSessionId,
                keyId: keyId
            }
        },
        select: {
            id: true,
            status: true
        }
    });
    if (existingMessage) {
        // Message exists! Update status if changed, but DO NOT re-trigger webhooks/bot
        if (fromMe && existingMessage.status !== 'SENT') {
            await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].message.update({
                where: {
                    id: existingMessage.id
                },
                data: {
                    status: 'SENT'
                }
            });
        }
        // Return null to indicate "Not New"
        return null;
    }
    // Debug fromMe issue (Keep this for a while)
    if (fromMe === undefined || fromMe === null) {
        console.log(`[DEBUG] Message ${keyId} has fromMe=${fromMe}. Key:`, JSON.stringify(msg.key));
    }
    const messageContent = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$messages$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["normalizeMessageContent"])(msg.message);
    let text = "";
    let messageType = "TEXT";
    // Extract content based on message type
    if (messageContent?.conversation) {
        text = messageContent.conversation;
    } else if (messageContent?.extendedTextMessage?.text) {
        text = messageContent.extendedTextMessage.text;
    } else if (messageContent?.imageMessage) {
        messageType = "IMAGE";
        text = messageContent.imageMessage.caption || "";
    } else if (messageContent?.videoMessage) {
        messageType = "VIDEO";
        text = messageContent.videoMessage.caption || "";
    } else if (messageContent?.audioMessage) {
        messageType = "AUDIO";
    } else if (messageContent?.documentMessage) {
        messageType = "DOCUMENT";
        text = messageContent.documentMessage.fileName || "";
    } else if (messageContent?.stickerMessage) {
        messageType = "STICKER";
    } else if (messageContent?.locationMessage) {
        messageType = "LOCATION";
        text = `${messageContent.locationMessage.degreesLatitude},${messageContent.locationMessage.degreesLongitude}`;
    } else if (messageContent?.contactMessage) {
        messageType = "CONTACT";
        text = messageContent.contactMessage.displayName || "";
    }
    // Determine effective participant for groups
    // Determine effective participant for groups with standard logic
    const isGroup = remoteJid.endsWith("@g.us");
    const remoteJidAlt = msg.key.remoteJidAlt; // LID/Phone JID handling
    let senderJid = fromMe ? undefined : isGroup ? msg.key.participant || msg.participant : remoteJid;
    // Prefer remoteJidAlt for DMs if available (matches webhook logic)
    if (!fromMe && !isGroup && remoteJidAlt) {
        senderJid = remoteJidAlt;
    }
    // --- Normalize LID JIDs to @s.whatsapp.net ---
    let normalizedRemoteJid = remoteJid;
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$jid$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["isLidJid"])(remoteJid)) {
        normalizedRemoteJid = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$jid$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["resolveToPhoneJid"])(remoteJid, dbSessionId, remoteJidAlt);
    }
    if (senderJid && (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$jid$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["isLidJid"])(senderJid)) {
        senderJid = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$jid$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["resolveToPhoneJid"])(senderJid, dbSessionId, remoteJidAlt);
    }
    // Download Media First (to save URL to DB)
    let fileUrl = null;
    try {
        fileUrl = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$webhook$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["downloadAndSaveMedia"])(msg, sessionId);
    } catch (e) {
        console.error("Error downloading media in store", e);
    }
    const newMessage = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].message.create({
        data: {
            sessionId: dbSessionId,
            remoteJid: (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$jid$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["normalizeJid"])(normalizedRemoteJid),
            senderJid,
            fromMe: fromMe || false,
            keyId,
            pushName,
            type: messageType,
            content: text,
            mediaUrl: fileUrl,
            status: fromMe ? "SENT" : "PENDING",
            timestamp
        }
    });
    // Ensure contact exists (Upsert Contact)
    const finalRemoteJid = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$jid$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["normalizeJid"])(normalizedRemoteJid);
    if (remoteJid && !remoteJid.includes('@g.us') && !remoteJid.includes('status@broadcast')) {
        const contactJid = finalRemoteJid; // Use fully normalized JID
        const contactData = {
            sessionId: dbSessionId,
            jid: contactJid
        };
        // Only update name/notify if message is FROM the contact (not from me)
        if (!fromMe) {
            if (pushName) contactData.notify = pushName;
            if (pushName) contactData.name = pushName;
        }
        const contact = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].contact.upsert({
            where: {
                sessionId_jid: {
                    sessionId: dbSessionId,
                    jid: contactJid
                }
            },
            create: {
                sessionId: dbSessionId,
                jid: contactJid,
                notify: !fromMe ? pushName : undefined,
                name: !fromMe ? pushName : undefined,
                // @ts-ignore
                remoteJidAlt: remoteJidAlt || undefined
            },
            update: !fromMe ? {
                notify: pushName,
                // @ts-ignore
                remoteJidAlt: remoteJidAlt || undefined
            } : {}
        });
        // Welcome Message Logic
        if (!fromMe && triggerWebhook && config?.welcomeMessage && sock) {
            // Check if this is a first-time interaction (contact created now)
            // Or if we want to be more specific, check message counts.
            // For simplicity, if the contact was just created (Prisma upsert returns the object).
            // But we can't easily tell if it was 'created' or 'updated' from upsert result without checking timestamps or using separate calls.
            // Let's check if message count for this contact is exactly 1 (the one we just saved)
            const msgCount = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].message.count({
                where: {
                    sessionId: dbSessionId,
                    remoteJid: finalRemoteJid
                }
            });
            if (msgCount === 1) {
                console.log(`Sending welcome message to ${finalRemoteJid}`);
                await sock.sendMessage(finalRemoteJid, {
                    text: config.welcomeMessage
                });
            }
        }
    }
    // Trigger webhook for new messages only (not history sync)
    // AND filter duplicates is implicitly done because we return 'false' above if existing
    if (triggerWebhook) {
        if (fromMe) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$webhook$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["onMessageSent"])(sessionId, msg, fileUrl).catch((e)=>console.error("Error in onMessageSent", e));
        } else {
            // Pass the fileUrl we just downloaded
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$webhook$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["onMessageReceived"])(sessionId, msg, fileUrl).catch((e)=>console.error("Error in onMessageReceived", e));
        }
    }
    return newMessage; // Is New Message = True (Return Object)
} // Placeholder - verified that I need to find the logic first
__turbopack_async_result__();
} catch(e) { __turbopack_async_result__(e); } }, false);}),
"[project]/src/modules/whatsapp/store/groups.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "syncGroups",
    ()=>syncGroups
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/prisma.ts [app-route] (ecmascript)");
;
async function syncGroups(sock, sessionId) {
    try {
        // Verify session exists and get the actual database ID
        const session = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findUnique({
            where: {
                sessionId
            },
            select: {
                id: true
            }
        });
        if (!session) {
            console.error(`Session ${sessionId} does not exist, cannot sync groups`);
            return;
        }
        // Use session.id (cuid) for foreign key, not sessionId string
        const dbSessionId = session.id;
        const groups = await sock.groupFetchAllParticipating();
        const groupList = Object.values(groups);
        console.log(`Found ${groupList.length} groups for session ${sessionId}`);
        for (const g of groupList){
            try {
                await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].group.upsert({
                    where: {
                        sessionId_jid: {
                            sessionId: dbSessionId,
                            jid: g.id
                        }
                    },
                    create: {
                        sessionId: dbSessionId,
                        jid: g.id,
                        subject: g.subject,
                        description: g.desc,
                        ownerJid: g.owner,
                        creation: g.creation ? new Date(g.creation * 1000) : undefined,
                        restrict: g.restrict,
                        announce: g.announce,
                        participants: g.participants,
                        metadata: g
                    },
                    update: {
                        subject: g.subject,
                        description: g.desc,
                        ownerJid: g.owner,
                        restrict: g.restrict,
                        announce: g.announce,
                        participants: g.participants,
                        metadata: g
                    }
                });
            } catch (e) {
                console.error(`Failed to sync group ${g.id}`, e);
            }
        }
        console.log(`Synced ${groupList.length} groups for session ${sessionId}`);
    } catch (e) {
        console.error("Failed to sync groups", e);
    }
}
}),
"[project]/src/modules/whatsapp/store/contacts.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "bindContactSync",
    ()=>bindContactSync
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/prisma.ts [app-route] (ecmascript)");
;
function bindContactSync(sock, sessionId) {
    // First, get the database Session ID (cuid)
    let dbSessionId = null;
    // Initialize by fetching the session ID
    (async ()=>{
        const session = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findUnique({
            where: {
                sessionId
            },
            select: {
                id: true
            }
        });
        if (session) {
            dbSessionId = session.id;
            console.log(`Contact sync initialized for session ${sessionId} (db: ${dbSessionId})`);
        } else {
            console.error(`Session ${sessionId} not found for contact sync`);
        }
    })();
    // Handle contacts.update event (fires when contacts are updated)
    sock.ev.on('contacts.update', async (updates)=>{
        if (!dbSessionId) {
            const session = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findUnique({
                where: {
                    sessionId
                },
                select: {
                    id: true
                }
            });
            if (!session) return;
            dbSessionId = session.id;
        }
        console.log(`Received ${updates.length} contact updates for session ${sessionId}`);
        for (const update of updates){
            try {
                if (!update.id) continue;
                await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].contact.upsert({
                    where: {
                        sessionId_jid: {
                            sessionId: dbSessionId,
                            jid: update.id
                        }
                    },
                    create: {
                        sessionId: dbSessionId,
                        jid: update.id,
                        name: update.name || update.notify,
                        notify: update.notify,
                        profilePic: update.imgUrl
                    },
                    update: {
                        name: update.name || undefined,
                        notify: update.notify || undefined,
                        profilePic: update.imgUrl || undefined
                    }
                });
            } catch (e) {
                console.error(`Failed to sync contact ${update.id}`, e);
            }
        }
    });
    // Also listen for messaging events to auto-create contacts
    sock.ev.on('messaging-history.set', async ({ chats, contacts, messages, isLatest })=>{
        if (!dbSessionId) {
            const session = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findUnique({
                where: {
                    sessionId
                },
                select: {
                    id: true
                }
            });
            if (!session) return;
            dbSessionId = session.id;
        }
        console.log(`Received messaging history: ${chats.length} chats, ${contacts?.length || 0} contacts, ${messages.length} messages`);
        // Sync chats as contacts (for personal chats) and groups
        for (const chat of chats){
            try {
                if (!chat.id || chat.id.includes('@broadcast')) continue;
                if (chat.id.includes('@g.us')) {
                    // Rescue group from history sync
                    await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].group.upsert({
                        where: {
                            sessionId_jid: {
                                sessionId: dbSessionId,
                                jid: chat.id
                            }
                        },
                        create: {
                            sessionId: dbSessionId,
                            jid: chat.id,
                            subject: chat.name || "Unknown Group"
                        },
                        update: {
                            subject: chat.name || undefined
                        }
                    });
                    continue; // Skip putting it into contacts
                }
                await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].contact.upsert({
                    where: {
                        sessionId_jid: {
                            sessionId: dbSessionId,
                            jid: chat.id
                        }
                    },
                    create: {
                        sessionId: dbSessionId,
                        jid: chat.id,
                        name: chat.name || undefined,
                        notify: chat.notify || undefined
                    },
                    update: {
                        name: chat.name || undefined
                    }
                });
            } catch (e) {
                console.error(`Failed to sync chat contact ${chat.id}`, e);
            }
        }
        // Sync explicit contacts
        if (contacts) {
            for (const contact of contacts){
                try {
                    if (!contact.id) continue;
                    await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].contact.upsert({
                        where: {
                            sessionId_jid: {
                                sessionId: dbSessionId,
                                jid: contact.id
                            }
                        },
                        create: {
                            sessionId: dbSessionId,
                            jid: contact.id,
                            name: contact.name || contact.notify,
                            notify: contact.notify
                        },
                        update: {
                            name: contact.name || undefined,
                            notify: contact.notify || undefined
                        }
                    });
                } catch (e) {
                    console.error(`Failed to sync contact ${contact.id}`, e);
                }
            }
        }
        console.log(`Synced contacts from messaging history for session ${sessionId}`);
    });
}
}),
"[project]/src/modules/whatsapp/store/autoreply.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

return __turbopack_context__.a(async (__turbopack_handle_async_dependencies__, __turbopack_async_result__) => { try {

__turbopack_context__.s([
    "bindAutoReply",
    ()=>bindAutoReply
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/prisma.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/@whiskeysockets/baileys/lib/index.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$messages$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@whiskeysockets/baileys/lib/Utils/messages.js [app-route] (ecmascript)");
var __turbopack_async_dependencies__ = __turbopack_handle_async_dependencies__([
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__,
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$messages$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__
]);
[__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$messages$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__] = __turbopack_async_dependencies__.then ? (await __turbopack_async_dependencies__)() : __turbopack_async_dependencies__;
;
;
// Helper for permission check (Deduplicate from command-handler if possible, but keep simple here)
function canAutoReply(config, fromMe, senderJid) {
    if (!config || !config.enabled) return false;
    // Auto Reply Specific Mode
    const mode = config.autoReplyMode || 'ALL';
    if (fromMe) {
        // If mode is OWNER, it triggers for ME? 
        // Auto Reply usually replies TO someone. 
        // If I send a message, and mode is OWNER, should it reply to me? 
        // User requested "Self Mode" -> Use case: Snippets.
        // So yes, if fromMe checks out.
        // However, standard auto-reply logic (replying to incoming) should be blocked if fromMe is true AND mode is ALL?
        // No, typically Auto Reply doesn't trigger on own messages to prevent unexpected loops.
        // But for "Self Mode" (Macros), it MUST trigger on own messages.
        if (mode === 'OWNER') return true;
        if (mode === 'ALL') return false; // Standard auto-reply ignores self
        // Specific? 
        return false;
    } else {
        // Incoming message from others
        if (mode === 'OWNER') return false; // Owner only acts on Owner messages
        if (mode === 'ALL') return true;
        if (mode === 'SPECIFIC') {
            const allowedJids = config.autoReplyAllowedJids || [];
            if (Array.isArray(allowedJids)) {
                return allowedJids.some((jid)=>senderJid.includes(jid));
            }
        }
        if (mode === 'BLACKLIST') {
            const blockedJids = config.autoReplyBlockedJids || [];
            if (Array.isArray(blockedJids)) {
                const isBlocked = blockedJids.some((jid)=>senderJid.includes(jid));
                return !isBlocked; // Return true if NOT blocked
            }
            return true; // If blacklist empty, allow all
        }
    }
    return false;
}
async function bindAutoReply(sock, sessionId) {
    sock.ev.on('messages.upsert', async ({ messages, type })=>{
        if (type !== 'notify') return;
        // Fetch session ID and Bot Config once per batch (optimization)
        const session = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findUnique({
            where: {
                sessionId
            },
            // @ts-ignore
            include: {
                botConfig: true
            }
        });
        if (!session) return;
        // @ts-ignore
        let config = session.botConfig;
        if (!config) {
            console.log("AutoReply: No config found, creating default...");
            config = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].botConfig.create({
                data: {
                    sessionId: session.id,
                    enabled: true,
                    botMode: 'OWNER',
                    autoReplyMode: 'ALL'
                }
            });
        }
        console.log(`AutoReply: Processing for ${sessionId}. Config:`, config ? "Found" : "Missing", config?.enabled ? "Enabled" : "Disabled");
        if (!config || !config.enabled) return;
        for (const msg of messages){
            const fromMe = msg.key.fromMe || false;
            const remoteJid = msg.key.remoteJid;
            // Standardized Sender Logic
            const isGroup = remoteJid?.endsWith("@g.us") || false;
            const remoteJidAlt = msg.key.remoteJidAlt;
            let senderJid = isGroup ? msg.key.participant || msg.participant : remoteJid;
            if (!isGroup && remoteJidAlt) {
                senderJid = remoteJidAlt;
            }
            if (!remoteJid || !senderJid) continue;
            // Check Permissions
            if (!canAutoReply(config, fromMe, senderJid)) continue;
            const content = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$messages$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["normalizeMessageContent"])(msg.message);
            const text = content?.conversation || content?.extendedTextMessage?.text || ""; // Caption?
            if (!text) continue;
            try {
                // Fetch rules for this session
                const rules = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].autoReply.findMany({
                    where: {
                        session: {
                            sessionId: sessionId
                        }
                    }
                });
                for (const rule of rules){
                    let match = false;
                    const keyword = rule.keyword.toLowerCase();
                    const incoming = text.toLowerCase();
                    switch(rule.matchType){
                        case 'EXACT':
                            match = incoming === keyword;
                            break;
                        case 'CONTAINS':
                            match = incoming.includes(keyword);
                            break;
                        case 'REGEX':
                            try {
                                const regex = new RegExp(rule.keyword, 'i');
                                match = regex.test(text); // Use original case for regex
                            } catch (e) {
                                console.error("Invalid regex in auto-reply", rule.keyword);
                            }
                            break;
                    }
                    if (match) {
                        // Check trigger context (GROUP, PRIVATE, or ALL)
                        const isGroup = remoteJid.endsWith('@g.us');
                        const triggerType = rule.triggerType || 'ALL'; // Default to ALL if undefined
                        if (triggerType === 'GROUP' && !isGroup) continue;
                        if (triggerType === 'PRIVATE' && isGroup) continue;
                        console.log(`Auto-reply match: ${rule.keyword} -> ${remoteJid}`);
                        if (rule.isMedia && rule.mediaUrl) {
                            const url = rule.mediaUrl;
                            const isVideo = url.endsWith('.mp4') || url.endsWith('.avi') || url.endsWith('.mov');
                            const isDocument = url.endsWith('.pdf') || url.endsWith('.doc') || url.endsWith('.docx') || url.endsWith('.zip');
                            if (isVideo) {
                                await sock.sendMessage(remoteJid, {
                                    video: {
                                        url
                                    },
                                    caption: rule.response
                                }, {
                                    quoted: msg
                                });
                            } else if (isDocument) {
                                await sock.sendMessage(remoteJid, {
                                    document: {
                                        url
                                    },
                                    caption: rule.response,
                                    mimetype: 'application/octet-stream',
                                    fileName: url.split('/').pop() || 'document'
                                }, {
                                    quoted: msg
                                });
                            } else {
                                // Default to Image
                                await sock.sendMessage(remoteJid, {
                                    image: {
                                        url
                                    },
                                    caption: rule.response
                                }, {
                                    quoted: msg
                                });
                            }
                        } else {
                            await sock.sendMessage(remoteJid, {
                                text: rule.response
                            }, {
                                quoted: msg
                            });
                        }
                        break;
                    }
                }
            } catch (e) {
                console.error("Auto-reply error", e);
            }
        }
    });
}
__turbopack_async_result__();
} catch(e) { __turbopack_async_result__(e); } }, false);}),
"[project]/src/modules/whatsapp/store/ppguard.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "bindPpGuard",
    ()=>bindPpGuard
]);
async function bindPpGuard(sock, sessionId) {
    sock.ev.on('contacts.update', async (updates)=>{
        for (const update of updates){
            if (update.imgUrl) {
                console.log(`Contact ${update.id} changed profile pic to ${update.imgUrl}`);
            // Verify logic: Fetch old imgUrl from DB and compare?
            // For now, simpler: Just notify self if it's a specific target?
            // Or just log it.
            // To properly implement Guard, we need to store 'lastProfilePic' in Contact table.
            // For "Fun" feature, let's just send a message to self
            // const me = sock.user?.id;
            // if (me) {
            //    await sock.sendMessage(me, { text: `Contact ${update.id} changed PP!` });
            // }
            }
        }
    });
}
}),
"[project]/src/modules/whatsapp/antispam.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "antispam",
    ()=>antispam
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/prisma.ts [app-route] (ecmascript)");
;
class AntiSpamManager {
    static instance;
    sessionHistory = new Map();
    configCache = new Map();
    processing = new Map();
    queues = new Map();
    messageCounter = 0;
    constructor(){}
    static getInstance() {
        if (!AntiSpamManager.instance) {
            AntiSpamManager.instance = new AntiSpamManager();
        }
        return AntiSpamManager.instance;
    }
    /**
     * Enqueue a message and wait for it to be processed.
     * Returns only when it's this message's turn to send.
     */ async enqueue(sessionId, jid, content) {
        const config = await this.getAntiSpamConfig(sessionId);
        if (!config || !config.antiSpamEnabled) {
            return; // Anti-spam disabled, send immediately
        }
        const messageType = this.detectMessageType(content);
        const msgId = ++this.messageCounter;
        return new Promise((resolve, reject)=>{
            const item = {
                id: msgId,
                sessionId,
                jid,
                messageType,
                queuedAt: Date.now(),
                resolve,
                reject
            };
            if (!this.queues.has(sessionId)) {
                this.queues.set(sessionId, []);
            }
            this.queues.get(sessionId).push(item);
            const queue = this.queues.get(sessionId);
            const position = queue.length;
            console.log(`[Anti-Spam] 📥 QUEUED  | Session: ${sessionId} | #${msgId} | To: ${this.formatJid(jid)} | Type: ${messageType} | Queue position: ${position}`);
            // Start processing if not already running
            this.processQueue(sessionId);
        });
    }
    async processQueue(sessionId) {
        if (this.processing.get(sessionId)) return; // Already processing
        this.processing.set(sessionId, true);
        const config = await this.getAntiSpamConfig(sessionId);
        if (!config) {
            this.processing.set(sessionId, false);
            return;
        }
        while(true){
            const queue = this.queues.get(sessionId);
            if (!queue || queue.length === 0) break;
            const item = queue[0]; // Peek at front
            const now = Date.now();
            const history = this.sessionHistory.get(sessionId) || [];
            // Clean history: keep only messages in the current window
            const windowStart = now - config.spamInterval * 1000;
            const recentMessages = history.filter((ts)=>ts > windowStart);
            this.sessionHistory.set(sessionId, recentMessages);
            if (recentMessages.length >= config.spamLimit) {
                // Rate limit reached — calculate and apply delay
                const delay = Math.floor(Math.random() * (config.spamDelayMax - config.spamDelayMin + 1)) + config.spamDelayMin;
                const sendAt = new Date(now + delay);
                const waitingSince = now - item.queuedAt;
                console.log(`[Anti-Spam] ⏳ DELAY   | Session: ${sessionId} | #${item.id} | Rate: ${recentMessages.length}/${config.spamLimit} in ${config.spamInterval}s | Delay: ${delay}ms | Send at: ${sendAt.toLocaleTimeString()} | Waiting: ${waitingSince}ms | Queue: ${queue.length} remaining`);
                await this.sleep(delay);
                // Re-fetch config (might have changed during delay)
                const freshConfig = await this.getAntiSpamConfig(sessionId);
                if (!freshConfig || !freshConfig.antiSpamEnabled) {
                    // Anti-spam was disabled during delay, flush all
                    console.log(`[Anti-Spam] ⚡ DISABLED | Session: ${sessionId} | Flushing ${queue.length} queued messages immediately`);
                    while(queue.length > 0){
                        const q = queue.shift();
                        this.recordSend(sessionId);
                        q.resolve();
                    }
                    break;
                }
                continue; // Re-check rate after delay
            }
            // Rate is within limit — send this message
            queue.shift(); // Remove from queue
            this.recordSend(sessionId);
            const totalWait = now - item.queuedAt;
            const remainingInQueue = queue.length;
            if (totalWait > 50) {
                console.log(`[Anti-Spam] ✅ SENDING | Session: ${sessionId} | #${item.id} | To: ${this.formatJid(item.jid)} | Type: ${item.messageType} | Waited: ${totalWait}ms | Queue: ${remainingInQueue} remaining`);
            } else {
                console.log(`[Anti-Spam] ✅ INSTANT | Session: ${sessionId} | #${item.id} | To: ${this.formatJid(item.jid)} | Type: ${item.messageType} | Rate: ${recentMessages.length + 1}/${config.spamLimit} | Queue: ${remainingInQueue} remaining`);
            }
            item.resolve();
        }
        this.processing.set(sessionId, false);
    }
    recordSend(sessionId) {
        const history = this.sessionHistory.get(sessionId) || [];
        history.push(Date.now());
        // Keep only last 60 seconds of history
        this.sessionHistory.set(sessionId, history.filter((ts)=>ts > Date.now() - 60000));
    }
    detectMessageType(content) {
        if (!content) return "unknown";
        if (content.text) return "text";
        if (content.image) return "image";
        if (content.video) return "video";
        if (content.audio) return "audio";
        if (content.document) return "document";
        if (content.sticker) return "sticker";
        if (content.react) return "reaction";
        if (content.delete) return "delete";
        if (content.poll) return "poll";
        if (content.location) return "location";
        if (content.contact) return "contact";
        return "other";
    }
    formatJid(jid) {
        if (jid.endsWith("@s.whatsapp.net")) {
            return jid.replace("@s.whatsapp.net", "");
        }
        if (jid.endsWith("@g.us")) {
            return `group:${jid.replace("@g.us", "").slice(-6)}`;
        }
        return jid.slice(0, 15);
    }
    sleep(ms) {
        return new Promise((resolve)=>setTimeout(resolve, ms));
    }
    /**
     * Fetch anti-spam config with 10-second cache
     */ async getAntiSpamConfig(sessionId) {
        const cached = this.configCache.get(sessionId);
        if (cached && Date.now() - cached.cachedAt < 10000) {
            return cached.config;
        }
        try {
            const results = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].$queryRawUnsafe(`SELECT bc.antiSpamEnabled, bc.spamLimit, bc.spamInterval, bc.spamDelayMin, bc.spamDelayMax
                 FROM BotConfig bc
                 INNER JOIN Session s ON s.id = bc.sessionId
                 WHERE s.sessionId = ?`, sessionId);
            if (results.length === 0) {
                this.configCache.set(sessionId, {
                    config: null,
                    cachedAt: Date.now()
                });
                return null;
            }
            const row = results[0];
            const config = {
                antiSpamEnabled: Boolean(row.antiSpamEnabled),
                spamLimit: Number(row.spamLimit) || 5,
                spamInterval: Number(row.spamInterval) || 10,
                spamDelayMin: Number(row.spamDelayMin) || 1000,
                spamDelayMax: Number(row.spamDelayMax) || 3000
            };
            this.configCache.set(sessionId, {
                config,
                cachedAt: Date.now()
            });
            return config;
        } catch (error) {
            console.error(`[Anti-Spam] ❌ ERROR   | Config fetch failed for ${sessionId}:`, error);
            this.configCache.set(sessionId, {
                config: null,
                cachedAt: Date.now()
            });
            return null;
        }
    }
    /** Clear cache for a session (call when config is updated) */ clearCache(sessionId) {
        this.configCache.delete(sessionId);
    }
}
const antispam = AntiSpamManager.getInstance();
}),
"[project]/src/modules/whatsapp/instance.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

return __turbopack_context__.a(async (__turbopack_handle_async_dependencies__, __turbopack_async_result__) => { try {

__turbopack_context__.s([
    "WhatsAppInstance",
    ()=>WhatsAppInstance
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/@whiskeysockets/baileys/lib/index.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Types$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@whiskeysockets/baileys/lib/Types/index.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$generics$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@whiskeysockets/baileys/lib/Utils/generics.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$auth$2d$utils$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@whiskeysockets/baileys/lib/Utils/auth-utils.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/prisma.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$auth$2f$usePrismaAuthState$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/modules/whatsapp/auth/usePrismaAuthState.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$pino__$5b$external$5d$__$28$pino$2c$__cjs$2c$__$5b$project$5d2f$node_modules$2f$pino$29$__ = __turbopack_context__.i("[externals]/pino [external] (pino, cjs, [project]/node_modules/pino)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$store$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/modules/whatsapp/store/index.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$store$2f$groups$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/modules/whatsapp/store/groups.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$store$2f$contacts$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/modules/whatsapp/store/contacts.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$store$2f$autoreply$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/modules/whatsapp/store/autoreply.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$store$2f$ppguard$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/modules/whatsapp/store/ppguard.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$antispam$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/modules/whatsapp/antispam.ts [app-route] (ecmascript)");
var __turbopack_async_dependencies__ = __turbopack_handle_async_dependencies__([
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__,
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$generics$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__,
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$auth$2d$utils$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__,
    __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$auth$2f$usePrismaAuthState$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__,
    __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$store$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__,
    __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$store$2f$autoreply$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__
]);
[__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$generics$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$auth$2d$utils$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$auth$2f$usePrismaAuthState$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$store$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$store$2f$autoreply$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__] = __turbopack_async_dependencies__.then ? (await __turbopack_async_dependencies__)() : __turbopack_async_dependencies__;
;
;
;
;
;
;
;
;
;
;
class WhatsAppInstance {
    socket = null;
    qr = null;
    rq = null;
    status = "DISCONNECTED";
    sessionId;
    userId;
    io;
    config = {};
    startTime = null;
    pairingCode = null;
    isStopped = false;
    constructor(sessionId, userId, io){
        this.sessionId = sessionId;
        this.userId = userId;
        this.io = io;
    }
    async init() {
        const sessionData = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findUnique({
            where: {
                sessionId: this.sessionId
            },
            include: {
                botConfig: true
            }
        });
        this.config = sessionData?.config || {};
        const botConfig = sessionData?.botConfig;
        const { state, saveCreds } = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$auth$2f$usePrismaAuthState$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["usePrismaAuthState"])(this.sessionId);
        const { version } = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$generics$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["fetchLatestBaileysVersion"])();
        this.socket = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["default"])({
            version,
            logger: (0, __TURBOPACK__imported__module__$5b$externals$5d2f$pino__$5b$external$5d$__$28$pino$2c$__cjs$2c$__$5b$project$5d2f$node_modules$2f$pino$29$__["default"])({
                level: process.env.BAILEYS_LOG_LEVEL || "error"
            }),
            printQRInTerminal: false,
            auth: {
                creds: state.creds,
                keys: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Utils$2f$auth$2d$utils$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["makeCacheableSignalKeyStore"])(state.keys, (0, __TURBOPACK__imported__module__$5b$externals$5d2f$pino__$5b$external$5d$__$28$pino$2c$__cjs$2c$__$5b$project$5d2f$node_modules$2f$pino$29$__["default"])({
                    level: process.env.BAILEYS_LOG_LEVEL || "error"
                }))
            },
            browser: [
                "Ubuntu",
                "Chrome",
                "20.0.04"
            ],
            markOnlineOnConnect: botConfig?.alwaysOnline ?? true,
            syncFullHistory: true
        });
        // Apply Anti-Spam Wrapper to sendMessage
        // This wraps the socket's sendMessage so ALL outgoing messages go through the queue
        const originalSendMessage = this.socket.sendMessage.bind(this.socket);
        const sessionId = this.sessionId;
        this.socket.sendMessage = async function(jid, content, options) {
            await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$antispam$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["antispam"].enqueue(sessionId, jid, content);
            return originalSendMessage(jid, content, options);
        };
        // Bind Store for DB Sync (handles incoming messages)
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$store$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["bindSessionStore"])(this.socket, this.sessionId, this.io);
        // Bind Contact Sync (handles contacts.update and messaging-history.set events)
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$store$2f$contacts$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["bindContactSync"])(this.socket, this.sessionId);
        this.socket.ev.on("creds.update", saveCreds);
        // Bind Group Sync (handles groups.upsert and groups.update)
        this.socket.ev.on("groups.upsert", async (groups)=>{
            try {
                const sessionDb = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findUnique({
                    where: {
                        sessionId: this.sessionId
                    },
                    select: {
                        id: true
                    }
                });
                if (!sessionDb) return;
                for (const g of groups){
                    await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].group.upsert({
                        where: {
                            sessionId_jid: {
                                sessionId: sessionDb.id,
                                jid: g.id
                            }
                        },
                        create: {
                            sessionId: sessionDb.id,
                            jid: g.id,
                            subject: g.subject,
                            description: g.desc,
                            ownerJid: g.owner,
                            creation: g.creation ? new Date(g.creation * 1000) : undefined,
                            restrict: g.restrict,
                            announce: g.announce,
                            participants: g.participants,
                            metadata: g
                        },
                        update: {
                            subject: g.subject,
                            description: g.desc,
                            ownerJid: g.owner,
                            restrict: g.restrict,
                            announce: g.announce,
                            participants: g.participants,
                            metadata: g
                        }
                    });
                }
            } catch (e) {
                console.error("Failed to upsert groups", e);
            }
        });
        this.socket.ev.on("groups.update", async (updates)=>{
            try {
                const sessionDb = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findUnique({
                    where: {
                        sessionId: this.sessionId
                    },
                    select: {
                        id: true
                    }
                });
                if (!sessionDb) return;
                for (const u of updates){
                    if (!u.id) continue;
                    // Only update fields that are provided
                    const updateData = {};
                    if (u.subject) updateData.subject = u.subject;
                    if (u.desc) updateData.description = u.desc;
                    if (u.owner) updateData.ownerJid = u.owner;
                    if (u.restrict !== undefined) updateData.restrict = u.restrict;
                    if (u.announce !== undefined) updateData.announce = u.announce;
                    if (Object.keys(updateData).length > 0) {
                        try {
                            await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].group.update({
                                where: {
                                    sessionId_jid: {
                                        sessionId: sessionDb.id,
                                        jid: u.id
                                    }
                                },
                                data: updateData
                            });
                        } catch (e) {
                        // Group might not exist yet, that's okay
                        }
                    }
                }
            } catch (e) {
                console.error("Failed to update groups", e);
            }
        });
        this.socket.ev.on("connection.update", async (update)=>{
            await this.handleConnectionUpdate(update);
        });
    }
    async handleConnectionUpdate(update) {
        const { connection, lastDisconnect, qr } = update;
        try {
            if (qr) {
                if (this.isStopped) return; // Don't emit QR if stopped
                this.qr = qr;
                this.status = "SCAN_QR";
                // Emit QR to Socket Room
                this.io?.to(this.sessionId).emit("connection.update", {
                    status: this.status,
                    qr
                });
                // Update DB
                await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.update({
                    where: {
                        sessionId: this.sessionId
                    },
                    data: {
                        qr,
                        status: "SCAN_QR"
                    }
                });
            }
            if (connection === "close") {
                const code = lastDisconnect?.error?.output?.statusCode;
                const isLoggedOut = code === __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$whiskeysockets$2f$baileys$2f$lib$2f$Types$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["DisconnectReason"].loggedOut;
                // Only reconnect if NOT logged out AND NOT explicitly stopped
                const shouldReconnect = !isLoggedOut && !this.isStopped;
                // Determine status based on reason
                if (isLoggedOut) {
                    this.status = "LOGGED_OUT";
                } else if (this.isStopped) {
                    this.status = "STOPPED";
                } else {
                    this.status = "DISCONNECTED";
                }
                this.io?.to(this.sessionId).emit("connection.update", {
                    status: this.status,
                    qr: null
                });
                // Use try-catch specifically for update as session might be deleted
                try {
                    await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.update({
                        where: {
                            sessionId: this.sessionId
                        },
                        data: {
                            status: this.status,
                            qr: null
                        }
                    });
                } catch (e) {
                // Ignore if session not found (deleted)
                }
                if (shouldReconnect) {
                    // Connection lost unexpectedly, reconnect
                    this.init();
                } else if (isLoggedOut) {
                    // Explicit logout: delete credentials
                    console.log(`Session ${this.sessionId} logged out. Deleting credentials...`);
                    try {
                        await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].$transaction([
                            __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.update({
                                where: {
                                    sessionId: this.sessionId
                                },
                                data: {
                                    status: "LOGGED_OUT",
                                    qr: null
                                }
                            }),
                            __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].authState.deleteMany({
                                where: {
                                    sessionId: this.sessionId
                                }
                            })
                        ]);
                    } catch (e) {}
                    this.socket = null;
                    this.config = {}; // Clear config cache
                    console.log(`Session ${this.sessionId} credentials deleted.`);
                } else if (this.isStopped) {
                    // Stopped: preserve credentials for future restart
                    console.log(`Session ${this.sessionId} stopped. Credentials preserved for auto-login.`);
                    this.socket = null;
                }
            }
            if (connection === "open") {
                this.status = "CONNECTED";
                this.qr = null;
                this.startTime = new Date();
                this.io?.to(this.sessionId).emit("connection.update", {
                    status: this.status,
                    qr: null
                });
                // Sync Groups from WhatsApp (with error handling)
                try {
                    await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$store$2f$groups$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["syncGroups"])(this.socket, this.sessionId);
                } catch (e) {
                    console.error("Group sync failed:", e);
                }
                // Bind Auto Reply
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$store$2f$autoreply$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["bindAutoReply"])(this.socket, this.sessionId);
                // Bind PP Guard
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$store$2f$ppguard$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["bindPpGuard"])(this.socket, this.sessionId);
                await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.update({
                    where: {
                        sessionId: this.sessionId
                    },
                    data: {
                        status: "CONNECTED",
                        qr: null
                    }
                });
                console.log(`Session ${this.sessionId} connected and synced successfully`);
            }
        } catch (error) {
            // Catch global errors in handler (like Record Not Found if session deleted mid-process)
            if (error.code === 'P2025') {
                console.warn(`Session ${this.sessionId} record not found during update. Stopping instance.`);
                this.socket?.end(undefined);
                this.socket = null;
            } else {
                console.error("Error in handleConnectionUpdate:", error);
            }
        }
    }
    async requestPairingCode(phoneNumber) {
        if (!this.socket) {
            throw new Error("Socket not initialized");
        }
        try {
            // Validate phone number (basic check)
            const cleanNumber = phoneNumber.replace(/[^0-9]/g, '');
            if (!cleanNumber) throw new Error("Invalid phone number");
            const code = await this.socket.requestPairingCode(cleanNumber);
            this.pairingCode = code;
            this.status = "SCAN_QR"; // Or specialized status? "PAIRING" is better but SCAN_QR triggers the right UI blocks usually
            // Emit update
            this.io?.to(this.sessionId).emit("connection.update", {
                status: this.status,
                qr: this.qr,
                pairingCode: code
            });
            return code;
        } catch (error) {
            console.error("Pairing code error:", error);
            throw error;
        }
    }
}
__turbopack_async_result__();
} catch(e) { __turbopack_async_result__(e); } }, false);}),
"[project]/src/lib/cron.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

return __turbopack_context__.a(async (__turbopack_handle_async_dependencies__, __turbopack_async_result__) => { try {

__turbopack_context__.s([
    "initScheduler",
    ()=>initScheduler
]);
var __TURBOPACK__imported__module__$5b$externals$5d2f$node$2d$cron__$5b$external$5d$__$28$node$2d$cron$2c$__esm_import$2c$__$5b$project$5d2f$node_modules$2f$node$2d$cron$29$__ = __turbopack_context__.i("[externals]/node-cron [external] (node-cron, esm_import, [project]/node_modules/node-cron)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/prisma.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$manager$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/modules/whatsapp/manager.ts [app-route] (ecmascript)");
var __turbopack_async_dependencies__ = __turbopack_handle_async_dependencies__([
    __TURBOPACK__imported__module__$5b$externals$5d2f$node$2d$cron__$5b$external$5d$__$28$node$2d$cron$2c$__esm_import$2c$__$5b$project$5d2f$node_modules$2f$node$2d$cron$29$__,
    __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$manager$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__
]);
[__TURBOPACK__imported__module__$5b$externals$5d2f$node$2d$cron__$5b$external$5d$__$28$node$2d$cron$2c$__esm_import$2c$__$5b$project$5d2f$node_modules$2f$node$2d$cron$29$__, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$manager$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__] = __turbopack_async_dependencies__.then ? (await __turbopack_async_dependencies__)() : __turbopack_async_dependencies__;
;
;
;
function initScheduler() {
    // Run every minute
    __TURBOPACK__imported__module__$5b$externals$5d2f$node$2d$cron__$5b$external$5d$__$28$node$2d$cron$2c$__esm_import$2c$__$5b$project$5d2f$node_modules$2f$node$2d$cron$29$__["default"].schedule("* * * * *", async ()=>{
        try {
            const now = new Date();
            // Fetch pending messages due for sending
            const pendingMessages = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].scheduledMessage.findMany({
                where: {
                    status: "PENDING",
                    sendAt: {
                        lte: now
                    }
                },
                include: {
                    session: true
                }
            });
            if (pendingMessages.length === 0) return;
            console.log(`Scheduler: Found ${pendingMessages.length} messages to send`);
            for (const msg of pendingMessages){
                const instance = __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$manager$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["waManager"].getInstance(msg.session.sessionId);
                if (!instance || !instance.socket) {
                    console.log(`Scheduler: Session ${msg.session.sessionId} not connected. Skipping.`);
                    continue;
                }
                try {
                    console.log(`Scheduler: Sending to ${msg.jid}`);
                    if (msg.mediaUrl) {
                        // TODO: Handle media sending
                        await instance.socket.sendMessage(msg.jid, {
                            text: msg.content // Caption for now if mediaUrl exists but logic not fully impl
                        });
                    } else {
                        await instance.socket.sendMessage(msg.jid, {
                            text: msg.content
                        });
                    }
                    // Update status
                    await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].scheduledMessage.update({
                        where: {
                            id: msg.id
                        },
                        data: {
                            status: "SENT"
                        }
                    });
                } catch (error) {
                    console.error(`Scheduler: Check failed for ${msg.id}`, error);
                    await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].scheduledMessage.update({
                        where: {
                            id: msg.id
                        },
                        data: {
                            status: "FAILED"
                        } // Failed to send
                    });
                }
            }
        } catch (error) {
            console.error("Scheduler error:", error);
        }
    });
    console.log("Scheduler initialized");
}
__turbopack_async_result__();
} catch(e) { __turbopack_async_result__(e); } }, false);}),
"[project]/src/modules/whatsapp/manager.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

return __turbopack_context__.a(async (__turbopack_handle_async_dependencies__, __turbopack_async_result__) => { try {

__turbopack_context__.s([
    "WhatsAppManager",
    ()=>WhatsAppManager,
    "waManager",
    ()=>waManager
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/prisma.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$instance$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/modules/whatsapp/instance.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$cron$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/cron.ts [app-route] (ecmascript)");
var __turbopack_async_dependencies__ = __turbopack_handle_async_dependencies__([
    __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$instance$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__,
    __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$cron$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__
]);
[__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$instance$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$cron$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__] = __turbopack_async_dependencies__.then ? (await __turbopack_async_dependencies__)() : __turbopack_async_dependencies__;
;
;
;
class WhatsAppManager {
    static instance;
    sessions = new Map();
    io = null;
    constructor(){
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$cron$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["initScheduler"])();
    }
    static getInstance() {
        if (!WhatsAppManager.instance) {
            WhatsAppManager.instance = new WhatsAppManager();
        }
        return WhatsAppManager.instance;
    }
    setup(io) {
        this.io = io;
    }
    async loadSessions() {
        if (!this.io) throw new Error("Socket.IO not initialized in WhatsAppManager");
        const sessions = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findMany({
            where: {
                status: {
                    not: "LOGGED_OUT"
                }
            }
        });
        for (const session of sessions){
            const instance = new __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$instance$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["WhatsAppInstance"](session.sessionId, session.userId, this.io);
            this.sessions.set(session.sessionId, instance);
            await instance.init();
        }
        console.log(`Loaded ${sessions.length} sessions.`);
    }
    async createSession(userId, name, customSessionId) {
        // Fallback to global IO if instance IO is missing (Next.js Context Issue)
        if (!this.io && /*TURBOPACK member replacement*/ __turbopack_context__.g.io) {
            this.io = /*TURBOPACK member replacement*/ __turbopack_context__.g.io;
        }
        if (!this.io) {
            console.error("Socket.IO not initialized in WhatsAppManager, and global fallback failed.");
            throw new Error("Socket.IO not initialized");
        }
        // Use custom ID if provided, otherwise generate random
        const sessionId = customSessionId || Math.random().toString(36).substring(7);
        const session = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.create({
            data: {
                userId,
                name,
                sessionId,
                status: "DISCONNECTED",
                botConfig: {
                    create: {
                        enabled: true,
                        botMode: "OWNER",
                        autoReplyMode: "ALL"
                    }
                }
            }
        });
        const instance = new __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$instance$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["WhatsAppInstance"](sessionId, userId, this.io);
        this.sessions.set(sessionId, instance);
        await instance.init();
        return session;
    }
    getInstance(sessionId) {
        return this.sessions.get(sessionId);
    }
    async deleteSession(sessionId) {
        const instance = this.sessions.get(sessionId);
        if (instance) {
            // Logout/Close socket
            instance.socket?.end(undefined);
            this.sessions.delete(sessionId);
        }
        await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.delete({
            where: {
                sessionId
            }
        });
    }
    async stopSession(sessionId) {
        const instance = this.sessions.get(sessionId);
        if (instance) {
            instance.isStopped = true; // Prevent auto-reconnect
            instance.socket?.end(undefined);
            instance.status = "STOPPED";
            this.io?.to(sessionId).emit("connection.update", {
                status: "STOPPED",
                qr: null
            });
            await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.update({
                where: {
                    sessionId
                },
                data: {
                    status: "STOPPED"
                }
            });
        }
    }
    async startSession(sessionId) {
        // If already running, do nothing
        const existingInstance = this.sessions.get(sessionId);
        if (existingInstance && existingInstance.status === "CONNECTED") {
            return;
        }
        const session = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findUnique({
            where: {
                sessionId
            }
        });
        if (!session) throw new Error("Session not found");
        // Re-initialize
        let instance = this.sessions.get(sessionId);
        if (!instance) {
            instance = new __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$instance$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["WhatsAppInstance"](sessionId, session.userId, this.io);
            this.sessions.set(sessionId, instance);
        }
        await instance.init();
    }
    async restartSession(sessionId) {
        await this.stopSession(sessionId);
        // Small delay to ensure cleanup
        await new Promise((resolve)=>setTimeout(resolve, 1000));
        await this.startSession(sessionId);
    }
    async requestPairingCode(sessionId, phoneNumber) {
        const instance = this.sessions.get(sessionId);
        if (!instance) throw new Error("Instance not found or not running");
        return await instance.requestPairingCode(phoneNumber);
    }
}
const globalForWhatsapp = /*TURBOPACK member replacement*/ __turbopack_context__.g;
const waManager = globalForWhatsapp.waManager || WhatsAppManager.getInstance();
// Always store in global to ensure singleton across Next.js compilations/chunks
globalForWhatsapp.waManager = waManager;
__turbopack_async_result__();
} catch(e) { __turbopack_async_result__(e); } }, false);}),
"[externals]/next/dist/server/app-render/action-async-storage.external.js [external] (next/dist/server/app-render/action-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/action-async-storage.external.js", () => require("next/dist/server/app-render/action-async-storage.external.js"));

module.exports = mod;
}),
"[project]/src/auth.config.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "authConfig",
    ()=>authConfig
]);
const authConfig = {
    pages: {
        signIn: '/auth/login'
    },
    callbacks: {
        authorized ({ auth, request: { nextUrl } }) {
            const isLoggedIn = !!auth?.user;
            const isOnDashboard = nextUrl.pathname.startsWith('/dashboard');
            if (isOnDashboard) {
                if (isLoggedIn) return true;
                return false; // Redirect unauthenticated users to login page
            } else if (isLoggedIn && nextUrl.pathname === '/auth/login') {
                return Response.redirect(new URL('/dashboard', nextUrl));
            }
            return true;
        },
        async jwt ({ token, user }) {
            if (user) {
                token.id = user.id;
                token.role = user.role;
            }
            return token;
        },
        async session ({ session, token }) {
            if (token && session.user) {
                session.user.id = token.id;
                session.user.role = token.role;
            }
            return session;
        }
    },
    providers: [],
    session: {
        strategy: 'jwt'
    },
    trustHost: true
};
}),
"[project]/src/lib/auth.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

return __turbopack_context__.a(async (__turbopack_handle_async_dependencies__, __turbopack_async_result__) => { try {

__turbopack_context__.s([
    "auth",
    ()=>auth,
    "handlers",
    ()=>handlers,
    "signIn",
    ()=>signIn,
    "signOut",
    ()=>signOut
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2d$auth$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/next-auth/index.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2d$auth$2f$providers$2f$credentials$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/next-auth/providers/credentials.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$auth$2f$core$2f$providers$2f$credentials$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@auth/core/providers/credentials.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/prisma.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$bcryptjs__$5b$external$5d$__$28$bcryptjs$2c$__esm_import$2c$__$5b$project$5d2f$node_modules$2f$bcryptjs$29$__ = __turbopack_context__.i("[externals]/bcryptjs [external] (bcryptjs, esm_import, [project]/node_modules/bcryptjs)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v4$2f$classic$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__ = __turbopack_context__.i("[project]/node_modules/zod/v4/classic/external.js [app-route] (ecmascript) <export * as z>");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$auth$2e$config$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/auth.config.ts [app-route] (ecmascript)");
var __turbopack_async_dependencies__ = __turbopack_handle_async_dependencies__([
    __TURBOPACK__imported__module__$5b$externals$5d2f$bcryptjs__$5b$external$5d$__$28$bcryptjs$2c$__esm_import$2c$__$5b$project$5d2f$node_modules$2f$bcryptjs$29$__
]);
[__TURBOPACK__imported__module__$5b$externals$5d2f$bcryptjs__$5b$external$5d$__$28$bcryptjs$2c$__esm_import$2c$__$5b$project$5d2f$node_modules$2f$bcryptjs$29$__] = __turbopack_async_dependencies__.then ? (await __turbopack_async_dependencies__)() : __turbopack_async_dependencies__;
;
;
;
;
;
;
const { handlers, signIn, signOut, auth } = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2d$auth$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["default"])({
    ...__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$auth$2e$config$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["authConfig"],
    providers: [
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$auth$2f$core$2f$providers$2f$credentials$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"])({
            credentials: {
                email: {
                    label: "Email",
                    type: "email"
                },
                password: {
                    label: "Password",
                    type: "password"
                }
            },
            authorize: async (credentials)=>{
                const parsedCredentials = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v4$2f$classic$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].object({
                    email: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v4$2f$classic$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().email(),
                    password: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v4$2f$classic$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().min(6)
                }).safeParse(credentials);
                if (parsedCredentials.success) {
                    const { email, password } = parsedCredentials.data;
                    const user = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].user.findUnique({
                        where: {
                            email
                        }
                    });
                    if (!user) return null;
                    // For the test user created via API which has plain password "password123":
                    // In real app, ALWAYS use hash. 
                    // Check if password matches hash OR plaintext (for transition/testing)
                    const passwordsMatch = await __TURBOPACK__imported__module__$5b$externals$5d2f$bcryptjs__$5b$external$5d$__$28$bcryptjs$2c$__esm_import$2c$__$5b$project$5d2f$node_modules$2f$bcryptjs$29$__["default"].compare(password, user.password) || password === user.password;
                    if (passwordsMatch) {
                        return user;
                    }
                }
                return null;
            }
        })
    ],
    secret: process.env.AUTH_SECRET || "secret"
});
__turbopack_async_result__();
} catch(e) { __turbopack_async_result__(e); } }, false);}),
"[project]/src/lib/api-auth.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

return __turbopack_context__.a(async (__turbopack_handle_async_dependencies__, __turbopack_async_result__) => { try {

__turbopack_context__.s([
    "canAccessSession",
    ()=>canAccessSession,
    "generateApiKey",
    ()=>generateApiKey,
    "getAccessibleSessions",
    ()=>getAccessibleSessions,
    "getAuthenticatedUser",
    ()=>getAuthenticatedUser,
    "hasRole",
    ()=>hasRole,
    "isAdmin",
    ()=>isAdmin,
    "isSessionOwner",
    ()=>isSessionOwner,
    "validateApiKey",
    ()=>validateApiKey
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/prisma.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/auth.ts [app-route] (ecmascript)");
var __turbopack_async_dependencies__ = __turbopack_handle_async_dependencies__([
    __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__
]);
[__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__] = __turbopack_async_dependencies__.then ? (await __turbopack_async_dependencies__)() : __turbopack_async_dependencies__;
;
;
// Role hierarchy for permission checks
const ROLE_HIERARCHY = {
    SUPERADMIN: 3,
    OWNER: 2,
    STAFF: 1
};
async function validateApiKey(request) {
    const apiKey = request.headers.get("x-api-key");
    if (!apiKey) {
        return null;
    }
    try {
        const user = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].user.findUnique({
            where: {
                apiKey
            },
            select: {
                id: true,
                email: true,
                name: true,
                role: true
            }
        });
        return user;
    } catch (error) {
        console.error("API key validation error:", error);
        return null;
    }
}
async function getAuthenticatedUser(request) {
    // First try API key if request is provided
    if (request) {
        const apiKeyUser = await validateApiKey(request);
        if (apiKeyUser) {
            return {
                ...apiKeyUser,
                authMethod: "apiKey"
            };
        }
    }
    // Fall back to session auth
    const session = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["auth"])();
    if (session?.user?.id) {
        // Fetch full user data including role
        const user = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].user.findUnique({
            where: {
                id: session.user.id
            },
            select: {
                id: true,
                email: true,
                name: true,
                role: true
            }
        });
        if (user) {
            return {
                ...user,
                authMethod: "session"
            };
        }
    }
    return null;
}
function hasRole(userRole, requiredRole) {
    const userLevel = ROLE_HIERARCHY[userRole] || 0;
    const requiredLevel = ROLE_HIERARCHY[requiredRole] || 0;
    return userLevel >= requiredLevel;
}
function isAdmin(userRole) {
    return userRole === "SUPERADMIN";
}
async function canAccessSession(userId, userRole, sessionId) {
    if (isAdmin(userRole)) {
        return true;
    }
    // Check if session belongs to user (ownership)
    const session = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findFirst({
        where: {
            OR: [
                {
                    id: sessionId,
                    userId
                },
                {
                    sessionId: sessionId,
                    userId
                }
            ]
        }
    });
    if (session) return true;
    // Check if user has shared access
    const dbSession = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findFirst({
        where: {
            OR: [
                {
                    id: sessionId
                },
                {
                    sessionId: sessionId
                }
            ]
        },
        select: {
            id: true
        }
    });
    if (!dbSession) return false;
    const sharedAccess = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].sessionAccess.findUnique({
        where: {
            sessionId_userId: {
                sessionId: dbSession.id,
                userId
            }
        }
    });
    return !!sharedAccess;
}
async function isSessionOwner(userId, userRole, sessionId) {
    if (isAdmin(userRole)) {
        return true;
    }
    const session = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findFirst({
        where: {
            OR: [
                {
                    id: sessionId,
                    userId
                },
                {
                    sessionId: sessionId,
                    userId
                }
            ]
        }
    });
    return !!session;
}
async function getAccessibleSessions(userId, userRole) {
    if (isAdmin(userRole)) {
        return __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findMany({
            orderBy: {
                createdAt: 'desc'
            },
            include: {
                botConfig: true,
                webhooks: true,
                _count: {
                    select: {
                        contacts: true,
                        messages: true,
                        groups: true,
                        autoReplies: true,
                        scheduledMessages: true
                    }
                }
            }
        });
    }
    // Get sessions owned by user + sessions shared with user
    const [ownedSessions, sharedAccess] = await Promise.all([
        __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findMany({
            where: {
                userId
            },
            orderBy: {
                createdAt: 'desc'
            },
            include: {
                botConfig: true,
                webhooks: true,
                _count: {
                    select: {
                        contacts: true,
                        messages: true,
                        groups: true,
                        autoReplies: true,
                        scheduledMessages: true
                    }
                }
            }
        }),
        __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].sessionAccess.findMany({
            where: {
                userId
            },
            select: {
                sessionId: true
            }
        })
    ]);
    if (sharedAccess.length === 0) return ownedSessions;
    const sharedSessionIds = sharedAccess.map((a)=>a.sessionId);
    const ownedIds = new Set(ownedSessions.map((s)=>s.id));
    const missingIds = sharedSessionIds.filter((id)=>!ownedIds.has(id));
    if (missingIds.length === 0) return ownedSessions;
    const sharedSessions = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findMany({
        where: {
            id: {
                in: missingIds
            }
        },
        orderBy: {
            createdAt: 'desc'
        },
        include: {
            botConfig: true,
            webhooks: true,
            _count: {
                select: {
                    contacts: true,
                    messages: true,
                    groups: true,
                    autoReplies: true,
                    scheduledMessages: true
                }
            }
        }
    });
    return [
        ...ownedSessions,
        ...sharedSessions
    ];
}
function generateApiKey() {
    const chars = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";
    let result = "wag_"; // Prefix for easy identification
    for(let i = 0; i < 32; i++){
        result += chars.charAt(Math.floor(Math.random() * chars.length));
    }
    return result;
}
__turbopack_async_result__();
} catch(e) { __turbopack_async_result__(e); } }, false);}),
"[project]/src/app/api/sessions/[sessionId]/settings/route.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

return __turbopack_context__.a(async (__turbopack_handle_async_dependencies__, __turbopack_async_result__) => { try {

__turbopack_context__.s([
    "DELETE",
    ()=>DELETE,
    "GET",
    ()=>GET,
    "PATCH",
    ()=>PATCH
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/prisma.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/server.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$manager$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/modules/whatsapp/manager.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$api$2d$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/api-auth.ts [app-route] (ecmascript)");
var __turbopack_async_dependencies__ = __turbopack_handle_async_dependencies__([
    __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$manager$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__,
    __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$api$2d$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__
]);
[__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$manager$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$api$2d$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__] = __turbopack_async_dependencies__.then ? (await __turbopack_async_dependencies__)() : __turbopack_async_dependencies__;
;
;
;
;
async function GET(request, { params }) {
    const { sessionId } = await params;
    try {
        const user = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$api$2d$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getAuthenticatedUser"])(request);
        if (!user) {
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                status: false,
                message: "Unauthorized",
                error: "Unauthorized"
            }, {
                status: 401
            });
        }
        const canAccess = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$api$2d$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["canAccessSession"])(user.id, user.role, sessionId);
        if (!canAccess) {
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                status: false,
                message: "Forbidden - Cannot access this session",
                error: "Forbidden - Cannot access this session"
            }, {
                status: 403
            });
        }
        const session = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.findUnique({
            where: {
                sessionId
            },
            select: {
                config: true
            }
        });
        if (!session) {
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                status: false,
                message: "Session not found",
                error: "Session not found"
            }, {
                status: 404
            });
        }
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            status: true,
            message: "Session settings fetched successfully",
            data: session
        });
    } catch (e) {
        console.error("Get session settings error:", e);
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            status: false,
            message: "Failed to get settings",
            error: "Failed to get settings"
        }, {
            status: 500
        });
    }
}
async function PATCH(request, { params }) {
    const { sessionId } = await params;
    try {
        const user = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$api$2d$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getAuthenticatedUser"])(request);
        if (!user) {
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                status: false,
                message: "Unauthorized",
                error: "Unauthorized"
            }, {
                status: 401
            });
        }
        // Check if user can access this session
        const canAccess = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$api$2d$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["canAccessSession"])(user.id, user.role, sessionId);
        if (!canAccess) {
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                status: false,
                message: "Forbidden - Cannot access this session",
                error: "Forbidden - Cannot access this session"
            }, {
                status: 403
            });
        }
        const body = await request.json();
        const { config } = body;
        const updated = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.update({
            where: {
                sessionId
            },
            data: {
                config
            }
        });
        // Update active instance if exists
        const instance = __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$manager$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["waManager"].getInstance(sessionId);
        if (instance) {
        // In a real app we might update internal instance state
        }
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            status: true,
            message: "Session settings updated successfully",
            data: updated
        });
    } catch (e) {
        console.error("Update session settings error:", e);
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            status: false,
            message: "Failed to update settings",
            error: "Failed to update settings"
        }, {
            status: 500
        });
    }
}
async function DELETE(request, { params }) {
    const { sessionId } = await params;
    try {
        const user = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$api$2d$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getAuthenticatedUser"])(request);
        if (!user) {
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                status: false,
                message: "Unauthorized",
                error: "Unauthorized"
            }, {
                status: 401
            });
        }
        // Check if user can access this session
        const canAccess = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$api$2d$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["canAccessSession"])(user.id, user.role, sessionId);
        if (!canAccess) {
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                status: false,
                message: "Forbidden - Cannot delete this session",
                error: "Forbidden - Cannot delete this session"
            }, {
                status: 403
            });
        }
        // Disconnect WhatsApp session first
        const instance = __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$manager$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["waManager"].getInstance(sessionId);
        if (instance?.socket) {
            try {
                await instance.socket.logout();
            } catch (e) {
                console.log("Session logout error (might be already disconnected):", e);
            }
        }
        __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$modules$2f$whatsapp$2f$manager$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["waManager"].deleteSession(sessionId);
        // Delete from database
        await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prisma"].session.delete({
            where: {
                sessionId
            }
        });
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            status: true,
            message: "Session deleted successfully"
        });
    } catch (e) {
        console.error("Delete session error:", e);
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            status: false,
            message: "Failed to delete session",
            error: "Failed to delete session"
        }, {
            status: 500
        });
    }
}
__turbopack_async_result__();
} catch(e) { __turbopack_async_result__(e); } }, false);}),
];

//# sourceMappingURL=%5Broot-of-the-server%5D__aba6ae0b._.js.map